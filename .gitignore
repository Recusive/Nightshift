**/__pycache__/
**/*.pyc
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Recursive session logs — raw JSONL (large, not needed in repo)
.recursive/sessions/raw/

# Runtime artifacts (product output)
Runtime/Nightshift/worktree-*/
Runtime/Nightshift/*.runner.log
Runtime/Nightshift/*.state.json

# Daemon runtime artifacts
.recursive-daemon.lock
