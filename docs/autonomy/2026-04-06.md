# Autonomy Report -- 2026-04-06

**Role**: ACHIEVE
**Dependency fixed**: Missing mandatory post-merge smoke validation on `main`
**Baseline score before fix**: 75/100
**Score after fix**: 80/100

## Baseline

AUTONOMY SCORE
==============
Self-Healing:    21/25  [5, 5, 3, 5, 3]
Self-Directing:  23/25  [5, 5, 5, 3, 5]
Self-Validating:  8/25  [5, 0, 0, 3, 0]
Self-Improving:  23/25  [5, 5, 5, 5, 3]
TOTAL:           75/100

Lowest category: Self-Validating at 8/25

## Evidence

- `docs/prompt/evolve.md` Step 9 previously checked only `gh run list --branch main --limit 1`; it did not require any dry-run after merge.
- `scripts/check.sh` already ran both `python3 -m nightshift run --dry-run --agent codex > /dev/null` and `python3 -m nightshift run --dry-run --agent claude > /dev/null`, so the repo already had the correct smoke commands before this session.
- `docs/tasks/0093.md` already tracked the exact missing post-merge smoke gate.
- `docs/sessions/index-review.md` still contains only "No reviewer sessions have been run yet."
- `docs/healer/log.md` and `docs/tasks/0124.md` still show weak snapshot/test-count evidence across handoff, tracker, and session-history surfaces.

## Fix Shipped

1. Updated `docs/prompt/evolve.md` Step 9 so post-merge health now requires both dry-runs on `main` after CI succeeds, and explicitly blocks session success if either fails.
2. Added a mirrored `SMOKE TEST RULE` to `docs/prompt/evolve-auto.md` so the autonomous builder cannot rationalize skipping the post-merge smoke check.
3. Added prompt-contract regression tests in `tests/test_nightshift.py` to lock the commands in both prompt files and the existing `scripts/check.sh` helper.
4. Closed task `#0093` because its acceptance criteria are now satisfied without introducing a second wrapper script.

## Verification

- `make check`
- `python3 -m nightshift run --dry-run --agent codex > /dev/null`
- `python3 -m nightshift run --dry-run --agent claude > /dev/null`

## Post-Fix Score

AUTONOMY SCORE
==============
Self-Healing:    21/25  [5, 5, 3, 5, 3]
Self-Directing:  23/25  [5, 5, 5, 3, 5]
Self-Validating: 13/25  [5, 0, 5, 3, 0]
Self-Improving:  23/25  [5, 5, 5, 5, 3]
TOTAL:           80/100

Lowest category: Self-Validating at 13/25

## Remaining Top Dependencies

1. Independent reviewer lane still has no durable runs (`docs/sessions/index-review.md`, task `#0107`). Impact: +5. Effort: large.
2. Snapshot and test-count consistency still depend on sparse docs/session metadata (`docs/tasks/0124.md`, `docs/tasks/0095.md`, `docs/tasks/0130.md`). Impact: +5. Effort: medium.
3. Main-branch CI failure recovery is documented but still only partially proven in practice (`docs/prompt/evolve-auto.md`, blocked umbrella `docs/tasks/0103.md`). Impact: +2. Effort: large.
