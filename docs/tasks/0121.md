---
status: pending
priority: normal
target: v0.0.8
vision_section: loop1
created: 2026-04-05
completed:
---

# Align cycle commit expectations with shift-log follow-up commits

Evaluation `#0010` reproduced a new verification failure on the final cycle:
`Cycle created 3 commits but structured output implies 0-1.` The run made one
real fix, then added separate shift-log commits, which means either the
structured output contract or `verify_cycle()` is miscounting a legitimate
pattern.

## Acceptance Criteria
- [ ] A reproduction matching Evaluation `#0010` no longer fails with `Cycle created 3 commits but structured output implies 0-1.`
- [ ] The fix preserves the guard rail against truly unexpected extra commits
- [ ] Regression coverage exercises a final cycle that emits one fix plus follow-up shift-log commits
