---
status: pending
priority: normal
target: v0.0.8
vision_section: none
created: 2026-04-05
completed:
---

# Fix malformed YAML in completed tasks and archive them

Tasks #0024 and #0036 have broken YAML frontmatter -- `## status: done` instead of `status: done` (markdown heading prefix leaked into the YAML block). This causes any automated parsing to misclassify them as non-done.

Both tasks are completed but remain in `docs/tasks/` instead of `docs/tasks/archive/`.

## Evidence

Handoff #0032 notes: "Tasks #0024 and #0036 have malformed YAML frontmatter." Issue persists across multiple sessions with no fix task created.

## Acceptance Criteria

- [ ] Fix YAML frontmatter in #0024 (change `## status: done` to `status: done`)
- [ ] Fix YAML frontmatter in #0036 (change `## status: done` to `status: done`)
- [ ] Move #0024 to `docs/tasks/archive/`
- [ ] Move #0036 to `docs/tasks/archive/`
