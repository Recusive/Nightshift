---
status: done
priority: normal
target: v0.0.8
vision_section: loop2
created: 2026-04-05
completed: 2026-04-05
duplicate_of: 0083
---

# Sub-agent coordination module for Loop 2

Loop 2 "Sub-agent coordination" is the ONLY remaining component at 0% in the tracker. Completing it would push Loop 2 to 100% and overall to ~91%.

This is a restatement of task #0083 with updated context: Loop 2 is now at 90% with all other components complete. The coordination module should:

1. Before spawning a wave, analyze estimated_files and acceptance_criteria to detect potential overlap between parallel tasks
2. Generate coordination hints for each sub-agent (e.g., "Task 3 is also modifying src/api/routes.py -- coordinate your changes")
3. Inject hints into work order prompts via the decomposer
4. After wave completion, check for file conflicts between completed tasks before integration

This does NOT require inter-agent communication. It is a pre-spawn analysis + post-spawn validation step.

## Acceptance Criteria
- [ ] `nightshift/coordination.py` exists with `detect_overlaps()` and `generate_coordination_hints()` functions
- [ ] Hints injected into work order prompts for overlapping tasks
- [ ] Post-wave file conflict detection before integration
- [ ] Tests cover overlap detection, hint generation, and conflict detection
- [ ] `make check` passes
