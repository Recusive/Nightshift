---
status: pending
priority: low
target: v0.0.9
vision_section: self-maintaining
created: 2026-04-05
source: review-pr-106-docs
completed:
---

# Keep module-map key symbols aligned with verifier-related helper changes

The docs review for PR #106 passed, but it noted that
`docs/architecture/MODULE_MAP.md` still summarizes `config.py` without showing
the verifier-related helpers added in recent sessions. The module map remains
usable, but the fast-orientation surface will drift if key-symbol extraction
lags behind the real public helper surface.

## Acceptance Criteria
- [ ] Update module-map generation so key symbols stay informative when modules
      gain new exported helpers related to the task at hand
- [ ] Regenerate `docs/architecture/MODULE_MAP.md` and confirm the `config.py`
      row reflects the current verifier-related surface
- [ ] Add regression coverage for the improved key-symbol selection behavior
