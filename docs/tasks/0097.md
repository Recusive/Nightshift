---
status: pending
priority: normal
target: v0.0.8
vision_section: self-maintaining
created: 2026-04-05
source: evaluation-0001
completed:
---

# Evaluation #0001: harden Claude evaluation startup

The first real Phractal evaluation did not start cleanly with the default command. `nightshift test --agent claude` inherited `CLAUDECODE=1`, which Claude interprets as a nested session, and then failed again because the default `claude_effort=max` setting is not supported in the current Claude subscription tier. The scored run only started after manual shell and config overrides.

Evaluation `#0003` did **not** reproduce that startup failure: the default
command started cleanly even with `CLAUDECODE=1` still present in the parent
session. This task now needs a reassessment step first so the queue does not
spend implementation effort on a stale toolchain issue.

## Acceptance Criteria
- [ ] Re-run the default Phractal evaluation startup path under the current Claude tooling and record whether `CLAUDECODE` or `claude_effort=max` still block startup
- [ ] If startup is now clean, close this task with evidence and remove stale startup-failure references from the active docs
- [ ] If startup still fails, evaluation/test runs sanitize nested Claude session env vars before invoking `claude`
- [ ] If startup still fails, unsupported Claude effort values are downgraded or validated before launch
- [ ] `python3 -m nightshift test --agent claude --cycles 1 --cycle-minutes 5` can start from a fresh Phractal clone without manual env/config edits
- [ ] Regression tests cover the env-sanitization and effort-normalization paths if code changes are required
