---
status: pending
priority: normal
target: v0.0.6
created: 2026-04-03
completed:
---

# Add plan CLI agent integration

The `nightshift plan` command currently generates the prompt and can parse results from a file. Extend it to actually invoke an agent (codex or claude) to generate the plan, then display the formatted result.

## Acceptance Criteria
- [ ] `nightshift plan "feature" --agent claude` runs the agent with the plan prompt
- [ ] Agent output is parsed and displayed as formatted markdown
- [ ] Scope warnings shown if plan exceeds limits
- [ ] `--dry-run` still works (no agent invocation)
- [ ] `--result-file` still works (no agent invocation)
- [ ] Tests cover agent invocation path (mocked)
