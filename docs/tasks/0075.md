---
status: pending
priority: low
target: v0.0.8
vision_section: none
created: 2026-04-05
completed:
---

# Deduplicate feature summary tasks #0059 and #0071

Tasks #0059 and #0071 both describe building a feature summary generation module for Loop 2. They have slightly different designs:
- #0059: `nightshift/summary.py` with `generate_feature_summary()`, stores summary in feature state under `summary` key
- #0071: `nightshift/feature_summary.py`, produces markdown summary, written to feature state directory

These should be consolidated into a single task. Keep #0059 (lower number, more detailed acceptance criteria). Archive #0071 with a note pointing to #0059.

## Acceptance Criteria

- [ ] Task #0071 is archived with `status: done` and a note: "Deduplicated -- see #0059"
- [ ] Task #0059 is updated to include the best parts of #0071's design (markdown output, partial completion handling)
- [ ] No duplicate feature summary tasks remain in the queue
