---
status: done
priority: normal
target: v0.0.8
vision_section: loop1
created: 2026-04-05
completed: 2026-04-06
---

# Isolate dry-run integration tests from runtime artifacts

`make check` failed this session because `TestDryRunIntegration` read a real
`docs/Nightshift/2026-04-05.state.json` file left by earlier runs, so the
dry-run prompt started at cycle 3 instead of cycle 1. Dry-run verification
should stay deterministic even when runtime artifacts already exist locally.

## Acceptance Criteria
- [ ] Dry-run integration tests set up or isolate their own `docs/Nightshift` state instead of depending on repo-global runtime artifacts
- [ ] `make check` passes both with and without pre-existing `docs/Nightshift/*.state.json` / `*.runner.log` artifacts
- [ ] Test coverage proves the prompt remains stable when stale runtime artifacts are present

## Closed

Done. Completed by PR #126 (`fix: isolate test-mode runtime artifacts`, commit 520d56c).
PR added `test_rejected_test_mode_run_keeps_target_repo_clean` and runtime-dir
isolation via `resolve_test_runtime_dir()`. Dry-run tests no longer depend on
repo-global state.
