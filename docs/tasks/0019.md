---
status: pending
priority: normal
target: v0.0.6
created: 2026-04-03
completed:
---

# Build task decomposer module

With the planner complete, build the decomposer -- the component that converts a FeaturePlan's task breakdown into individual sub-agent work orders. Each work order contains the task description, acceptance criteria, repo profile, full plan context, and the JSON schema the agent must return.

See docs/vision/02-loop2-feature-builder.md "Sub-Agent Execution" for spec.

## Acceptance Criteria
- [ ] `nightshift/decomposer.py` exists with `decompose_plan()` function
- [ ] Takes a FeaturePlan, returns list of work orders (new TypedDict)
- [ ] Work orders include repo profile context and task-specific prompt
- [ ] Respects execution_order() -- groups work orders by wave
- [ ] Tests cover decomposer logic
- [ ] Vision tracker updated
