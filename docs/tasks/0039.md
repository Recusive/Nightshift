---
status: pending
priority: low
target: v0.0.7
created: 2026-04-04
completed:
---

# Add Codex/OpenAI model pricing to cost tracker

The cost tracker currently has pricing for Claude models only (Opus, Sonnet, Haiku). Codex sessions use OpenAI models (gpt-5.4 etc.) whose pricing is not included. Token counts are tracked correctly but cost shows as $0.00 for unknown models.

Fix: add pricing entries for OpenAI models used by Codex to `MODEL_PRICING` in `constants.py`. Check Codex stream-json log format to confirm the usage field names match (may use `prompt_tokens`/`completion_tokens` instead of `input_tokens`/`output_tokens`).

## Acceptance Criteria
- [ ] At least gpt-5.4 has pricing in MODEL_PRICING
- [ ] Codex session logs parse correctly (verify field names)
- [ ] Cost calculation returns non-zero for Codex sessions
