---
status: pending
priority: normal
target: v0.0.8
created: 2026-04-05
completed:
---

# Update README.md to reflect current state

The README was rewritten in PR #42 but is already outdated. The system has shipped 10+ features since then (healer, human escalation, generate-work, cost tracking improvements, learnings verification, handoff compaction, etc.). Numbers, roadmap checkboxes, and architecture sections are stale.

## What to update

- **Numbers table**: test count, PR count, task count, learnings count, module count, commit count — pull from live data
- **Roadmap**: check off items that shipped since PR #42 (healer, human escalation, generate-work step, learnings verification, vision-alignment check)
- **Architecture tree**: add any new modules (compact.py, costs.py already listed — verify nothing else was added)
- **The 4 daemons section**: mention the healer now runs as a builder step, not a separate daemon call
- **Config section**: any new config fields added since PR #42
- **Guard rails section**: any new protections added
- **Session memory section**: mention the learnings INDEX.md system

## How to get current numbers

```bash
# Tests
python3 -m pytest tests/ -q --tb=no 2>&1 | tail -1

# PRs merged
gh pr list --state merged --json number | python3 -c "import json,sys; print(len(json.load(sys.stdin)))"

# Modules
ls nightshift/*.py | wc -l

# Source lines
wc -l nightshift/*.py | tail -1

# Tasks
ls docs/tasks/0*.md docs/tasks/archive/0*.md 2>/dev/null | wc -l

# Learnings
ls docs/learnings/2026-*.md | wc -l

# Commits
git log --oneline main | wc -l
```

## Acceptance Criteria

- [ ] All numbers in the README match live data
- [ ] Roadmap checkboxes reflect what's actually shipped
- [ ] Architecture tree matches actual module list
- [ ] No stale references to features that changed (e.g., healer as separate daemon)
- [ ] make check passes (README is docs-only, so just verify no formatting issues)
