---
status: done
priority: normal
target: v0.0.8
vision_section: self-maintaining
created: 2026-04-05
source: evaluation-0001
completed: 2026-04-06
---

# Evaluation #0001: leave evaluation clones clean after rejected cycles

After the rejected Phractal run, the worktree was reset but the target clone still had untracked `.nightshift.json` and `Docs/Nightshift/` artifacts. Evaluations should leave the target checkout clean even when cycles are rejected.

## Acceptance Criteria
- [ ] Evaluation temp config and runtime artifacts live outside the target repo or are fully cleaned up before exit
- [ ] A failed/rejected evaluation leaves `git status --short` clean in the cloned target repo
- [ ] Regression coverage exists for cleanup after rejected cycles
