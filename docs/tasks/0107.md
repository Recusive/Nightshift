---
status: wontfix
priority: normal
target: v0.0.9
vision_section: self-maintaining
created: 2026-04-05
completed:
---

# Activate reviewer daemon cadence and durable review indexing

The strategist report found that 58 PRs have merged while `docs/sessions/index-review.md` still shows no reviewer sessions. Builder-side review catches some issues, but there is no independent reviewer lane paying down quality debt across untouched files.

## Acceptance Criteria
- [ ] Define a concrete cadence for `scripts/daemon-review.sh` (for example daily, or every N merged PRs)
- [ ] The reviewer daemon writes durable entries to `docs/sessions/index-review.md` on every run
- [ ] `docs/ops/DAEMON.md` documents when to run the reviewer lane and how to confirm it actually ran
- [ ] Verification includes one real reviewer run that leaves an entry in `docs/sessions/index-review.md`

## Closed

Wontfix. Superseded by the unified daemon architecture (PR #123, `feat/overseer-rewrite`).
`scripts/daemon-review.sh` no longer exists. The review role is handled by `pick-role.py`
scoring, and review sessions are logged in the unified `docs/sessions/index.md` (see
sessions 20260406-040148, 20260406-080246). The separate `index-review.md` concept is
obsolete.

