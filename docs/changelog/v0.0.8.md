# v0.0.8 -- Self-Maintaining

**Released**: TBD
**Tag**: `v0.0.8`
**Status**: In progress

Closing the self-maintaining gap: auto-release, auto-changelog, evaluation CLI, and infrastructure automation.

## Added

## Changed
- **[feat]** Loop 2 repo profiling now scans nested `package.json` files, parses Python dependencies from `requirements.txt` / `pyproject.toml`, and infers naming/import conventions so planner and work-order prompts receive richer repo context.

## Fixed
- **[fix]** Shell scripts in `scripts/` now use ASCII-only section dividers and restart/status text, removing box-drawing and em-dash characters that violated repo conventions and rendered inconsistently across terminals/filesystems. (task #0038)

## Removed

## Internal
- **[meta]** Strategist reviews now include a `Prompt Health` section that samples the last 10 builder sessions, cites prompt file line references, and proposes concrete add/remove/reword prompt edits instead of vague prompt feedback. (task `#0050`)
- **[docs]** Documented the strategist `Prompt Health` workflow in `docs/ops/DAEMON.md` and recorded the first strategy report using it in `docs/strategy/2026-04-05.md`.
- **[test]** Added profiler and prompt coverage for dependency detection, monorepo manifests, convention inference, and the expanded `RepoProfile` schema. Test suite is now 890 passing.
- **[test]** Added `TestShellScripts::test_shell_scripts_are_ascii_only` and updated shell script headers/comments so `scripts/*.sh` stay ASCII-clean. Test suite is now 891 passing.
- **[meta]** Recorded the first real Phractal evaluation in `docs/evaluations/0001.md` and generated follow-up tasks `#0097`-`#0101` from the issues it exposed.
- **[test]** Added explicit `cache_read` pricing assertions for `gpt-5.4-mini` and `gpt-5.4-nano` in `TestCostConstants`, closing the remaining constant-coverage gap from task `#0041`.
- **[meta]** Recorded a second Phractal evaluation rerun in `docs/evaluations/0002.md` and updated the evaluation workflow to reuse existing pending tasks instead of generating duplicate follow-ups when the same low-scoring dimensions recur.
- **[test]** Added parity assertions tying `AGENT_DEFAULT_MODELS` to `DEFAULT_CONFIG["codex_model"]` and `DEFAULT_CONFIG["claude_model"]`, so default model drift now fails fast in `TestCostConstants` instead of silently skewing cost attribution. (task `#0042`)
- **[meta]** Recorded a third real Phractal evaluation in `docs/evaluations/0003.md`; the default Claude run now starts cleanly without manual overrides, but shift-log verification, missing verify-command wiring, clone cleanup, and rejected-cycle reporting/scoring gaps remain.
- **[refactor]** Replaced the loose `dict[str, str]` handoff-parsing contract in `compact.py` with a private `_ParsedHandoff` `TypedDict`, and added parser coverage so weekly handoff compaction stays mypy-safe. (task `#0044`)
- **[meta]** Recorded a fourth real Phractal evaluation in `docs/evaluations/0004.md`; startup stayed healthy without overrides, while the existing shift-log, verification, cleanup, and rejected-run reporting gaps reproduced again.
- **[meta]** Recorded a fifth real Phractal evaluation in `docs/evaluations/0005.md`; default Claude startup regressed in this environment, requiring an `env -u CLAUDECODE` rerun and a temporary `claude_effort: high` override before the same verification, cleanup, and rejected-run visibility gaps could be rescored.
