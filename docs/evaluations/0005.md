# Evaluation #0005

**Date**: 2026-04-05
**Target**: Phractal
**Agent**: claude
**Cycles**: 2
**After task**: #0044 (add `_ParsedHandoff` TypedDict for `compact.py`)

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Startup | 4/10 | The prescribed default run failed because the parent session exported `CLAUDECODE=1`, which Claude treated as a nested session. A second fresh-clone rerun still failed because Nightshift invoked `claude --effort max`, which this environment rejects. The scored rerun only started after unsetting `CLAUDECODE` and adding a temporary `claude_effort: high` override. |
| Discovery | 7/10 | The scored rerun found two plausible production issues in real backend code: incorrect prompt-truncation telemetry in `apps/api/app/security/prompt_validator.py` and internal error-detail leakage in `apps/api/app/api/v1/health.py`. |
| Fix quality | 6/10 | Both fixes were narrow and coherent, but neither added tests and both cycles were rejected before Nightshift could accept or verify them. |
| Shift log | 3/10 | The runner log and state file show real fix and shift-log commits, but the human-readable dated shift log stayed the untouched template and Cycle 2 also triggered a commit-count consistency violation. |
| State file | 8/10 | `docs/Nightshift/2026-04-05.state.json` is valid JSON and captures both rejected cycles, commit hashes, touched files, and the halt reason. |
| Verification | 2/10 | Baseline verification was still skipped because no target-specific verification command was configured, and both cycles were rejected before any meaningful repo verification ran. |
| Guard rails | 8/10 | File-count limits held, blocked paths stayed untouched, and the runner rejected invalid cycles instead of silently accepting them. |
| Clean state | 3/10 | The scored rerun finished dirty with untracked `.nightshift.json` and `Docs/Nightshift/` artifacts in the target clone. |
| Breadth | 6/10 | The run covered both shared security utilities and API endpoint code, which is broader than a single-file slice but still clustered in backend surfaces. |
| Usefulness | 4/10 | The runner log and state JSON preserve actionable findings, but the day-team-facing shift log remained a template and the top-level counters stayed at zero. |
| **Total** | **51/100** | |

## Tasks Created

- None new. Existing pending tasks already cover the low-scoring failures reproduced again here: `#0097`, `#0098`, `#0099`, `#0100`, `#0101`, and `#0102`.

## Raw Evidence

- Default run: `PYTHONPATH=<nightshift-repo> python3 -m nightshift test --agent claude --cycles 2 --cycle-minutes 5` from `/tmp/nightshift-eval-GiN5tD/repo` failed because Claude detected `CLAUDECODE=1` from the parent session.
- Second fresh-clone rerun: `env -u CLAUDECODE PYTHONPATH=<nightshift-repo> python3 -m nightshift test --agent claude --cycles 2 --cycle-minutes 5` from `/tmp/nightshift-eval-rerun-4fDteM/repo` still failed before a scorable run because Nightshift launched `claude --effort max`, which this Claude environment does not support.
- Scored rerun: a third fresh clone at `/tmp/nightshift-eval-scored-LMJmOy/repo` with `env -u CLAUDECODE` plus a temporary `.nightshift.json` override of `{ "claude_effort": "high" }`.
- Shift log: `/tmp/nightshift-eval-scored-LMJmOy/repo/docs/Nightshift/2026-04-05.md` remained the untouched template summary/stats despite runner-log evidence of real fix and shift-log commits.
- State file: `/tmp/nightshift-eval-scored-LMJmOy/repo/docs/Nightshift/2026-04-05.state.json` recorded both cycles as `rejected`, both with `verify_status: "skipped"`, Cycle 1 with a missing shift-log-recognition violation, and Cycle 2 with an additional commit-count mismatch violation.
- Runner log: `/tmp/nightshift-eval-scored-LMJmOy/repo/docs/Nightshift/2026-04-05.runner.log` captured Cycle 1 fix commit `5f4ddc2`, Cycle 2 fix commit `141175b`, and the separate shift-log-only commit `1a34d9a`.
- Clean-state check: `git status --short` in the scored temp clone showed untracked `.nightshift.json` and `Docs/Nightshift/` after the run.
