# Evaluation #0010

**Date**: 2026-04-05
**Target**: Phractal
**Agent**: claude
**Cycles**: 2
**After task**: #0117 (Step 0 evaluation targeting fix)

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Startup | 8/10 | The corrected default command started from the Nightshift repo root, created the worktree inside `/tmp/nightshift-eval`, and produced all expected artifacts without any override rerun. Baseline verification still skipped because `verify_command` remained null. |
| Discovery | 7/10 | The run found a concrete fix in `apps/api/app/security/prompt_validator.py` around misleading truncation logging, which is more than placeholder churn even though the run stayed narrow. |
| Fix quality | 6/10 | The actual code change was plausible and small in scope, but the session still ended in rejected cycles and the final-cycle structured output disagreed with the commit count. That kept the run from being a trustworthy accepted fix. |
| Shift log | 2/10 | The required `/tmp/nightshift-eval/docs/Nightshift/2026-04-05.md` file stayed a template while the real artifacts were written under `/tmp/nightshift-eval/Docs/Nightshift/...`, so the human-readable log still failed the primary review path. |
| State file | 8/10 | `/tmp/nightshift-eval/docs/Nightshift/2026-04-05.state.json` is valid JSON, records both rejected cycles, and captures the halt reason plus nested cycle findings. |
| Verification | 2/10 | Baseline verification was skipped again because `verify_command` was null, and both cycles were rejected before any repo-specific verification could provide a strong signal. |
| Guard rails | 7/10 | The runner stayed inside the cloned repo, respected the cycle limits, and rejected invalid cycles instead of silently accepting them. The extra-commit mismatch in the final-cycle output is still a guard-rail fidelity problem. |
| Clean state | 3/10 | The clone was left dirty with untracked `Docs/Nightshift/` artifacts after the rejected run. |
| Breadth | 5/10 | The run touched one backend security helper and the mistaken shift-log path, so it still explored only a narrow slice of the repo. |
| Usefulness | 5/10 | The runner log and state file are actionable, but the template shift log and rejected-run presentation still make the result harder for a day-team reviewer to trust or consume quickly. |
| **Total** | **53/100** | |

## Tasks Created

- None new. Existing pending tasks still cover the reproduced low-scoring failures: `#0098`, `#0099`, `#0100`, `#0101`, and `#0102`.

## Raw Evidence

- Default run: `PYTHONPATH=$(pwd) python3 -m nightshift test --agent claude --cycles 2 --cycle-minutes 5 --repo-dir /tmp/nightshift-eval`
- Shift log target path: `/tmp/nightshift-eval/docs/Nightshift/2026-04-05.md` remained on template text after the run.
- Actual runtime artifacts: `/tmp/nightshift-eval/Docs/Nightshift/2026-04-05.md`, `/tmp/nightshift-eval/Docs/Nightshift/2026-04-05.state.json`, `/tmp/nightshift-eval/Docs/Nightshift/2026-04-05.runner.log`
- State file observations: both cycles were `rejected`; cycle 1 stored a real fix for `apps/api/app/security/prompt_validator.py`; cycle 2 recorded violations `No commit in this cycle includes the shift log update.` and `Cycle created 3 commits but structured output implies 0-1.`
- Clean-state check: `git -C /tmp/nightshift-eval status --short` returned `?? Docs/Nightshift/`
