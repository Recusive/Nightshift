# Evaluation #0014

**Date**: 2026-04-05
**Target**: Phractal
**Agent**: claude
**Cycles**: 2
**After task**: #0131

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Startup | 8/10 | The prescribed default command started from the Nightshift repo root, created the worktree inside the fresh clone, and finished without any env or config overrides. Startup is still capped because baseline verification remained unavailable with `verify_command: null`. |
| Discovery | 7/10 | The run found two concrete middleware issues in the target repo and committed both fixes. Discovery is still limited by the absence of any separately logged issue, so the run did not leave broader follow-up findings beyond the fixes themselves. |
| Fix quality | 8/10 | Both fixes were focused, plausible, and low-blast-radius. Confidence is still capped by the missing repo verification command, so the evaluator cannot prove the target repo still passes its intended checks. |
| Shift log | 8/10 | `Docs/Nightshift/2026-04-05.md` exists, is human-readable, and includes matching commit hashes, impact, verification, and recommendations. It still lacks broader cleanliness context for the dirty clone. |
| State file | 9/10 | `Docs/Nightshift/2026-04-05.state.json` is valid JSON, records both cycles, and preserves counters plus per-cycle verification metadata. The only notable gap is that verification remained `skipped` throughout the run. |
| Verification | 3/10 | Baseline and cycle verification were skipped again because Phractal still has no target-specific evaluation verify command. This remains the main production-readiness gap in real-repo evaluations. |
| Guard rails | 10/10 | The runner stayed inside the clone, respected the cycle and file limits, and did not touch blocked paths. |
| Clean state | 3/10 | The cloned repo still ended dirty: `git -C /tmp/nightshift-eval status --short` returned `?? Docs/Nightshift/` after the run. |
| Breadth | 6/10 | The run covered two categories, but both accepted fixes stayed inside the `apps/api/app/middleware` slice and `recent_cycle_paths` never widened beyond `apps`. |
| Usefulness | 7/10 | A day-team reader could act on the shift log directly, and the startup evidence is now strong enough to close `#0097`. Skipped verification and the dirty clone still prevent the evaluation from being a trustworthy green signal. |
| **Total** | **69/100** | |

## Tasks Created

- None new. Existing task coverage already matches the reproduced failures:
  - `#0099` tracks the missing Phractal verification command.
  - `#0100` tracks evaluation-clone cleanup after runs.
  - `#0125` tracks the clean-state scoring gap for dirty clones.

## Raw Evidence

- Default run: `PYTHONPATH=$(pwd) python3 -m nightshift test --agent claude --cycles 2 --cycle-minutes 5 --repo-dir /tmp/nightshift-eval`
- Shift log: `/tmp/nightshift-eval/Docs/Nightshift/2026-04-05.md`
- State file: `/tmp/nightshift-eval/Docs/Nightshift/2026-04-05.state.json`
- Runner log: `/tmp/nightshift-eval/Docs/Nightshift/2026-04-05.runner.log`
- Clean-state check: `git -C /tmp/nightshift-eval status --short` returned `?? Docs/Nightshift/`
