# Evaluation #0006

**Date**: 2026-04-05
**Target**: Phractal
**Agent**: claude
**Cycles**: 2
**After task**: #0051 (cross-session cost intelligence)

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Startup | 4/10 | The prescribed default run still failed immediately because Claude inherited `CLAUDECODE=1`, then the runner still chose unsupported `--effort max`. A second fresh-clone rerun only became scorable after unsetting `CLAUDECODE` and adding a temporary `.nightshift.json` override of `{ "claude_effort": "high" }`. |
| Discovery | 7/10 | The scored rerun found one plausible auth-cache collision fix in `apps/api/app/middleware/fast_auth.py` plus three additional concrete logged issues in auth, error handling, and CORS code. |
| Fix quality | 6/10 | The JWT cache-key change is narrow and technically coherent, but it shipped without tests, without repo verification, and the cycle was still rejected for shift-log bookkeeping. |
| Shift log | 2/10 | The required `docs/Nightshift/2026-04-05.md` file stayed the untouched template, while the agent wrote a separate `Docs/Nightshift/2026-04-05.md` file instead. Both cycles were rejected with `No commit in this cycle includes the shift log update.` |
| State file | 7/10 | `docs/Nightshift/2026-04-05.state.json` is valid JSON and preserves nested rejected-cycle findings, but top-level counters stayed at zero and both cycles remained rejected. |
| Verification | 2/10 | Baseline verification was skipped again because `verify_command` stayed null, and no post-cycle verification ran before the run halted on rejected cycles. |
| Guard rails | 8/10 | The run stayed within file-count limits, avoided blocked paths, and rejected invalid cycles instead of silently accepting them. |
| Clean state | 3/10 | The scored rerun left untracked `.nightshift.json` and `Docs/Nightshift/` artifacts in the target clone. |
| Breadth | 5/10 | The run stayed concentrated in backend/auth surfaces plus docs; it found real issues, but not across meaningfully different top-level product areas. |
| Usefulness | 4/10 | The runner log and nested state data are actionable, but the human-facing shift log remained a template and the top-level counters still under-report the rejected findings. |
| **Total** | **48/100** | |

## Tasks Created

- None new. Existing pending tasks still cover the low-scoring failures reproduced here: `#0097`, `#0098`, `#0099`, `#0100`, `#0101`, and `#0102`.

## Raw Evidence

- Default run: `PYTHONPATH=/Users/no9labs/Developer/Recursive/Nightshift python3 -m nightshift test --agent claude --cycles 2 --cycle-minutes 5` from `/tmp/nightshift-eval-default-20260405` failed twice because Claude inherited `CLAUDECODE=1` and the runner invoked `claude --effort max`.
- Scored rerun: fresh clone at `/tmp/nightshift-eval-rerun-20260405` with `env -u CLAUDECODE` plus temporary `.nightshift.json` override `{ "claude_effort": "high" }`.
- Shift log: `/tmp/nightshift-eval-rerun-20260405/docs/Nightshift/2026-04-05.md` remained the untouched template, while the agent-created log landed at `/tmp/nightshift-eval-rerun-20260405/Docs/Nightshift/2026-04-05.md`.
- State file: `/tmp/nightshift-eval-rerun-20260405/docs/Nightshift/2026-04-05.state.json` recorded both cycles as `rejected`, with `verify_status: "skipped"` and violations for missing shift-log commit recognition.
- Runner log: `/tmp/nightshift-eval-rerun-20260405/docs/Nightshift/2026-04-05.runner.log` captured real fix commits `e1eed48` / `a2914ff`, shift-log commits `39e28a3` / `0f6b88a`, and the rejected-cycle summaries.
- Clean-state check: `git status --short` in `/tmp/nightshift-eval-rerun-20260405` showed untracked `.nightshift.json` and `Docs/Nightshift/` after the run.
