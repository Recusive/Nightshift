# Evaluation #0011

**Date**: 2026-04-05
**Target**: Phractal
**Agent**: claude
**Cycles**: 2
**After task**: #0118 (README accuracy refresh)

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Startup | 8/10 | The default command started cleanly from the Nightshift repo root, targeted the fresh clone, created the worktree, and wrote the expected state and runner artifacts without any rerun overrides. Baseline verification still skipped because `verify_command` stayed null. |
| Discovery | 8/10 | The run found two concrete backend issues in different files: a middleware error-handling bug and a broken CORS regex configuration. |
| Fix quality | 6/10 | Both fixes looked plausible and low-blast-radius, but the runner rejected the cycles because of Nightshift verification bugs, so the evaluation still cannot treat them as trusted shipped work. |
| Shift log | 3/10 | The advertised `docs/Nightshift/2026-04-05.md` file stayed on the template while the agent's real commits updated `Docs/Nightshift/2026-04-05.md`, so the primary human review path was still misleading. |
| State file | 8/10 | `/tmp/nightshift-eval.tk4T2h/docs/Nightshift/2026-04-05.state.json` is valid JSON, records both rejected cycles, and preserves the fix details plus verification failures. |
| Verification | 2/10 | Baseline verification was skipped again because `verify_command` was null, and the run then hit false verification failures instead of providing a reliable repo-specific signal. |
| Guard rails | 6/10 | The runner stayed inside the cloned repo and enforced the cycle limits, but it still falsely rejected legitimate shift-log behavior and over-counted a final-cycle follow-up commit. |
| Clean state | 3/10 | The clone was left dirty with untracked `Docs/Nightshift/` artifacts after the rejected run. |
| Breadth | 6/10 | The run covered two different backend areas plus the log path artifact, which is better than a single-directory sweep but still short of broad repo coverage. |
| Usefulness | 6/10 | The runner log and state file are actionable, but the stale template shift log means a day-team reviewer still has to inspect raw artifacts to understand what happened. |
| **Total** | **56/100** | |

## Tasks Created

- None new. Existing task coverage already matched the reproduced failures; this session then fixed `#0098` and `#0121`, leaving `#0099`, `#0100`, `#0101`, and `#0102` as the remaining evaluation follow-ups.

## Raw Evidence

- Default run: `PYTHONPATH=$(pwd) python3 -m nightshift test --agent claude --cycles 2 --cycle-minutes 5 --repo-dir /tmp/nightshift-eval.tk4T2h`
- Runner log showed real rejected-cycle output for:
  - `apps/api/app/middleware/error_handler.py` (`3316b9a`)
  - `apps/api/app/core/cors.py` (`eab5b9d`)
- State file observations: cycle 1 and cycle 2 were both `rejected`; cycle 1 recorded `No commit in this cycle includes the shift log update.` and cycle 2 added `Cycle created 3 commits but structured output implies 1-2.`
- Human-readable artifact mismatch: `/tmp/nightshift-eval.tk4T2h/docs/Nightshift/2026-04-05.md` stayed on template content while the rejected-cycle state referenced `Docs/Nightshift/2026-04-05.md`
- Clean-state check: `git -C /tmp/nightshift-eval.tk4T2h status --short` returned `?? Docs/Nightshift/`
