# Week 2026-W14 Summary
**Sessions**: 0001-0014
**Dates**: 2026-04-03
**Version**: v0.0.1 -> v0.0.5 released, v0.0.6 in progress

## Progress
- Loop 1: 0% -> 100%
- Loop 2: 0% -> 36%
- Self-Maintaining: 0% -> 54%
- Meta-Prompt: 0% -> 57%
- Overall: 0% -> 67%

## What Was Built
- Session 0001: Python orchestrator, pluggable agent adapters (Codex + Claude), worktree isolation, guard rails, verification, state tracking, shift log generation, 123 tests. v0.0.2 ready.
- Session 0002: v0.0.2 released. merge_config deep merge (security fix). run_command timeout fix.
- Session 0003: v0.0.2 tagged on GitHub. Post-cycle diff scorer with regex-based content analysis.
- Session 0004: Cycle-to-cycle state injection -- prior cycle intelligence in prompts.
- Session 0005: Test writing incentives -- counter, escalation directive, score bonus.
- Session 0006: Backend exploration forcing -- repo classification, frontend-heavy detection, steering directive.
- Session 0007: v0.0.3 released. Phractal validation (2-cycle test shift, system works end-to-end).
- Session 0008: Merged PR #13 (forbidden commands + backend forcing), relaxed shift-log verification.
- Session 0009: Category balancing directive -- steers agents toward underrepresented fix categories.
- Session 0010: v0.0.4 released. Multi-repo support with sequential runs and aggregate reporting.
- Session 0011: Repo profiler module -- detects language, frameworks, package manager, test runner, monorepo markers.
- Session 0012: Feature planner module -- topological sort for execution waves, plan validation, markdown rendering.
- Session 0013: Task decomposer -- converts feature plans into sub-agent work orders grouped by execution wave.
- Session 0014: Sub-agent spawner -- executes work orders via codex/claude CLI, parses JSON output, retries on failures.

## Decisions Still Active
- Always `gh pr merge --merge --delete-branch --admin` (never squash)
- GitHub releases include highlights AND full changelog
- Code structure rules: one concern per module, no hardcoded data in logic files, new module checklist
- Sub-agent spawner builds its own CLI commands (not reusing cycle.py command_for_agent)
- spawn_wave() runs tasks sequentially -- parallel execution is a future optimization
- Agent "blocked" status is NOT retried -- only parse failures trigger retries

## Bugs Still Open
- Task #0012 (Phractal re-validation) -- requires codex/claude CLI in environment
- Profiler only scans root package.json for monorepo deps (task #0018)

## Lessons Learned
- CI uses detached HEAD -- git tests must handle this
- mypy rejects .get() on required TypedDict fields -- use direct access
- ruff format must run before push (make check catches it)
- Turn budget of 100 is too low for full sessions -- use 500+
- tee buffers claude output -- use --output-format stream-json
- Daemon must check for open PRs before each cycle
- Code structure rules in CLAUDE.md catch real violations
- Codex skips shift-log-in-commit instructions
- extract_json has limits with deeply nested JSON in arbitrary text
- Reuse existing functions in the codebase before creating new ones
