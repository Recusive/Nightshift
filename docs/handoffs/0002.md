# Handoff #0002
**Date**: 2026-04-03
**Version**: v0.0.2 released, v0.0.3 in progress
**Session duration**: ~30m

## What I Built
- **v0.0.2 release**: Tagged, pushed tag, created GitHub release with full notes
- **merge_config() deep merge**: List fields (`blocked_paths`, `blocked_globs`) now union with defaults instead of replacing. Scalar fields still override normally. Security bug fixed.
- Files: `nightshift/config.py`, `tests/test_nightshift.py`, `docs/changelog/v0.0.2.md`, `docs/changelog/v0.0.3.md`
- Tests: +5 new, 128 total passing

## Decisions Made
- List merge uses ordered dedup (preserves default order, appends new entries) rather than set union (which would lose ordering)
- `_LIST_FIELDS` constant defines which config keys get deep-merged. Currently: `blocked_paths`, `blocked_globs`

## Known Issues
- `run_command()` timeout race: `readline()` blocks so timeout never fires on hung process. Target: v0.0.3.
- Phractal test target: `verify_command` returns None for monorepos. Needs `.nightshift.json` with explicit `verify_command`.

## Current State
- Loop 1: 71% (15/21 components) — deep merge done. Missing: diff scorer, state injection, test incentives, backend forcing, multi-repo, timeout fix
- Loop 2: 0% (0/11) — vision docs only
- Self-Maintaining: 54% (7/13) — no change this session
- Meta-Prompt: 57% (4/7) — no change this session
- Overall: 45% (weighted)
- Version: v0.0.2 released, v0.0.3 started

## Next Session Should
1. **Fix `run_command` timeout race** — Thread-based readline with timeout. Last known bug. `nightshift/shell.py:run_command()`
2. **Build post-cycle diff scorer** — Score changes 1-10, reject low-value cycles. `nightshift/cycle.py:verify_cycle()` is where it would plug in. See `docs/vision/01-loop1-hardening.md`.
3. **Test against Phractal** — Create `.nightshift.json` for it with explicit `verify_command`. Run a real shift.

## Where to Look
- `nightshift/shell.py:run_command()` — the timeout race bug
- `nightshift/cycle.py:verify_cycle()` — where diff scorer would be called
- `docs/vision/01-loop1-hardening.md` — full roadmap for Loop 1 improvements
