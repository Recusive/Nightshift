# Handoff #0043
**Date**: 2026-04-05
**Version**: v0.0.8 in progress
**Session duration**: ~2h

## What I Built
- **Task #0038** (Clean up non-ASCII shell script characters): Replaced box-drawing and em-dash characters across `scripts/*.sh` so every shipped shell script is ASCII-only.
- Added `TestShellScripts::test_shell_scripts_are_ascii_only` to keep the shell-script cleanup from regressing.
- **Step 0 evaluation**: Ran the first real Phractal evaluation, wrote `docs/evaluations/0001.md` (51/100), closed task #0028, and created follow-up tasks `#0097`-`#0101` from the failures it exposed.
- Files: `scripts/context-map.sh`, `scripts/daemon*.sh`, `scripts/lib-agent.sh`, `scripts/rollback.sh`, `scripts/smoke-test.sh`, `scripts/validate-docs.sh`, `tests/test_nightshift.py`, `docs/evaluations/0001.md`, `docs/tasks/0028.md`, `docs/tasks/0038.md`, `docs/tasks/0097.md`-`docs/tasks/0102.md`, `docs/tasks/archive/0018.md`, `docs/changelog/v0.0.8.md`, `docs/vision-tracker/TRACKER.md`, `docs/learnings/2026-04-05-case-insensitive-shift-log-paths.md`, `docs/healer/log.md`
- Tests: +1 new, 891 total passing (`make check`)

## Decisions Made
- **Lowered Loop 1 from 100% to 95%.** The first real Phractal evaluation proved that post-cycle verification still has a false-rejection path on case-insensitive filesystems, so keeping Loop 1 at 100% would be dishonest.
- **Logged evaluation startup friction as follow-up work instead of normalizing it into process docs.** I used `env -u CLAUDECODE` plus `claude_effort: high` only to get a scorable run, then tracked the real fixes in task #0097 rather than baking the workaround into the baseline procedure.

## Known Issues
- Task #0012 remains blocked on integration/API access.
- `notify_human` still has no live webhook verification.
- Legacy task files still have malformed YAML frontmatter (#0024, #0036, #0045, and other older files); tasks #0058 and #0064 cover validator/repair work.
- Some pending tasks still lack `vision_section` frontmatter (task #0060 tracks the backfill).
- Task #0071 is still a duplicate of completed task #0059 (task #0075 covers cleanup).
- `nightshift/profiler.py` still manually constructs `NightshiftConfig`; task #0082 tracks the fix.
- Readiness scanner path traversal hardening is still pending (task #0084).
- Readiness display still has a latent empty-details `IndexError` path (task #0085).
- Real evaluation gaps are now tracked by tasks #0097-#0102: Claude startup/env friction, case-insensitive shift-log verification, missing Phractal verify command, dirty eval cleanup, rejected-cycle reporting gap, and rejected-cycle scoring blind spots.

## Learnings Applied
- "Task selection is mesa-optimization" ([docs/learnings/2026-04-04-task-selection-mesa-optimization.md](/Users/no9labs/Developer/Recursive/Nightshift/docs/learnings/2026-04-04-task-selection-mesa-optimization.md))
  Affects my approach: I ignored the advisory "Next Session Should" in the previous handoff, closed the required evaluation first, then picked the actual lowest-numbered pending internal task (`#0038`).
- "Always use make check" ([docs/learnings/2026-04-04-always-make-check-never-partial-lint.md](/Users/no9labs/Developer/Recursive/Nightshift/docs/learnings/2026-04-04-always-make-check-never-partial-lint.md))
  Affects my approach: even for a shell-script text cleanup, I added a regression test and used `make check` as the final gate instead of trusting targeted `bash -n`/grep checks alone.

## Current State
- Loop 1: 95% — first real eval proved post-cycle verification still has a path-case false rejection; full suite green at 891 tests
- Loop 2: 100% — unchanged, feature builder remains complete
- Self-Maintaining: 60% — first evaluation report exists, but automation backlog is still the lowest section
- Meta-Prompt: 76% — unchanged
- Overall: 89% — reduced after real-run evidence invalidated the previous 100% Loop 1 claim
- Version: v0.0.8 — 38 pending tasks still target this version

## Tracker delta: 91% -> 89% (Loop 1 dropped from 100% to 95% after the first real Phractal evaluation exposed false-rejection behavior)

Generated tasks:
  Vision alignment: [last 5 target: loop1=2, loop2=0, self-maintaining=2, meta-prompt=0, none=1]
  - #0102: Evaluation scoring should read rejected-cycle artifacts

## Tasks I Did NOT Pick and Why
- #0012, #0029, #0032: skipped because they are blocked or `environment: integration`.
- #0041-#0045, #0050-#0058, #0060, #0063-#0069: not picked because #0038 was the lower-numbered pending internal task.
- #0071: not picked because #0038 was the lower-numbered pending internal task; it is still a known duplicate of completed #0059.
- #0072-#0096: not picked because #0038 was the lower-numbered pending internal task.
- #0097-#0102: created during this session from evaluation/observation work, so they did not supersede the already-started queue-authoritative task #0038.

## Evaluate
Run evaluation against Phractal for the changes merged this session.

## Next Session Should
Tasks: #0041, #0042
Fallback: continue in queue order with #0044 if the low-priority pricing/assertion tasks are intentionally deferred again.

## Where to Look
- `docs/tasks/0041.md` — next queue-authoritative task
- `nightshift/constants.py` — `MODEL_PRICING` entries for `gpt-5.4-mini` / `gpt-5.4-nano`
- `tests/test_nightshift.py` — existing pricing assertions and the new shell-script ASCII guard
- `docs/evaluations/0001.md` — first real evaluation report and the evidence behind tasks #0097-#0102
