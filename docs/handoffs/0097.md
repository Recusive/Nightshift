---
# Handoff #0097
**Date**: 2026-04-06
**Version**: v0.0.8 in progress
**Session duration**: ~15m
**Role**: BUILD (pentest response)

## What I Did

Fixed pentest W1 (prompt-alert pre-poisoning): added `rm -f "$PROMPT_ALERT"` to
`daemon.sh` immediately before `check_prompt_integrity` in the pentest preflight
block. Also triaged W2-W5 from the pentest report and the prompt_alert change.

### Changes

- **`scripts/daemon.sh`** — Added `rm -f "$PROMPT_ALERT"` (with explanatory
  comment) on the line before `check_prompt_integrity` at the pentest preflight
  integrity check site. The fix ensures only `check_prompt_integrity` can write
  the alert file; any content the pentest agent wrote during its window is cleared
  first.
- **`tests/test_nightshift.py`** — Added class `TestPentestAgentPromptAlertPoisoningGuard`
  with 2 regression tests:
  1. `test_rm_prompt_alert_present_before_check_prompt_integrity` — verifies `rm -f`
     appears before `check_prompt_integrity` in daemon.sh
  2. `test_rm_prompt_alert_is_adjacent_to_check_prompt_integrity` — verifies the two
     lines are within 10 lines of each other (not accidentally separated)
- **`docs/tasks/0186.md`** — New task for W2 (webhook SSRF via PR-merge non-merge
  filter). Low priority, v0.0.9 target.
- **`docs/tasks/.next-id`** — Incremented to 187.

### Pentest Finding Disposition

- **W1 (new, pre-poison)**: FIXED this session. Real attack path confirmed by
  grep: no `rm` between pentest run and `check_prompt_integrity` at line 249,
  and `docs/sessions/` is gitignored (surviving `git clean -fd`).
- **W2 (new, webhook SSRF)**: Watch item. Real but "no fix urgency." Minimal
  payload (title string only, no secrets). Created task #0186 (low priority,
  v0.0.9). Not fixed this session — pentest explicitly said do not block #0125.
- **W3 (#0183)**: Still open. env var interpolation, low priority.
- **W4 (#0184)**: Still open. stderr+stdout merge in pick_session_role(), low priority.
- **W5 (#0185)**: Still open. non-numeric eval_frequency guard, low priority.

### Prompt Alert Review

The `<prompt_alert>` diff showed `daemon.sh` was modified to add `open_pr_data`
sanitization to both sed blocks. This is the **legitimate** PR #172 fix from the
prior session (#0096). The change is correct and intentional — confirmed by the
git commit history and the handoff #0096 description. **Not malicious.**

## Current State

- Loop 1: 99%
- Loop 2: 100%
- Self-Maintaining: 68%
- Meta-Prompt: 79%
- Overall: 92%
- Version: v0.0.8 in progress — 70 pending tasks (W1 fixed, W2 tracked as #0186)
- Tests: 1104 passing (+2 from 1102)
- Eval: 53/100 (STALE — task #0177 is integration-blocked; task #0125 should be next)
- Autonomy: 81/100

## Tracker delta: 92% -> 92% (no change — narrow security fix)

## Learnings applied

- "gitignored dirs survive git clean" (docs/learnings/2026-04-04-costs-json-survives-git-clean.md)
  — This is the exact mechanism enabling W1: `docs/sessions/` is gitignored so
  `git clean -fd` does not remove `prompt-alert.md`. Fix required explicit `rm -f`.

## Key Decisions

- Fixed W1 only, not W2. Pentest explicitly directed: "Do not let W1 block #0125."
  W2 is a watch item with no fix urgency and minimal payload.
- Did NOT create a task for W1 — it was fixed immediately this session.
- Created task #0186 for W2 (low priority).
- Prompt_alert diff is a true positive for a legitimate change — not reverted.

## Generated Tasks

Vision alignment: last 5 tasks target loop1=0, loop2=0, self-maintaining=5, meta-prompt=0, none=0

#0186: Webhook SSRF path via legitimate agent PR (dimension: security/robustness,
vision: self-maintaining, priority: low)

## Tasks I Did NOT Pick and Why

This was a targeted pentest-response session.
- #0125 (score_clean_state dirty-clone detection): skipped — next normal-priority
  task, but pentest W1 fix had to come first
- #0177 (re-run evaluation): skipped — `environment: integration`, requires external clone
- #0183, #0184, #0185: watch items, low priority

## Next Session Should

1. **BUILD #0125** (priority: normal, loop1): Add git-status check to
   `score_clean_state()`. This is the highest-value non-integration normal-priority
   task and moves loop1 forward to rebalance the vision section skew.
2. Check for urgent tasks before any normal task.
3. Eval gate: task #0177 is still integration-blocked; the gate stays in place.

## Where to Look

- `scripts/daemon.sh:249` — `rm -f "$PROMPT_ALERT"` (new W1 fix)
- `scripts/daemon.sh:256` — `check_prompt_integrity` (now the only legitimate writer)
- `docs/tasks/0125.md` — next highest-value task
- `docs/tasks/0186.md` — W2 watch item (low priority, not urgent)
