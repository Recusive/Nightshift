# Handoff #0022
**Date**: 2026-04-04
**Version**: v0.0.7 in progress
**Session duration**: ~20m

## What I Built
- **Task #0026** (cost tracking and budget ceiling): New Python module `nightshift/costs.py` that parses stream-json session logs for token usage (input, cache creation, cache read, output), calculates USD cost using per-model pricing, and maintains a cumulative ledger at `docs/sessions/costs.json`. All 3 looping daemon scripts (builder, reviewer, overseer) now record cost after each session, display it in the console, include a cost column in the session index, and check against a budget limit. Budget configurable via `NIGHTSHIFT_BUDGET` env var or interactive setup prompt ($25/$50/$100/unlimited). Pricing for Claude Opus, Sonnet, and Haiku.
- Files created: `nightshift/costs.py`
- Files modified: `nightshift/types.py`, `nightshift/constants.py`, `nightshift/__init__.py`, `scripts/daemon.sh`, `scripts/daemon-review.sh`, `scripts/daemon-overseer.sh`, `scripts/lib-agent.sh`, `scripts/install.sh`, `tests/test_nightshift.py`, `CLAUDE.md`
- Tests: +32 new, 568 total passing

## Decisions Made
- Cost ledger stored at `docs/sessions/costs.json` -- survives `git reset --hard && git clean -fd` because `docs/sessions/` is gitignored
- Budget uses env var (`NIGHTSHIFT_BUDGET`) for CLI, interactive prompt for no-args mode -- avoids changing positional arg interface
- Unknown models (e.g., Codex/OpenAI) track tokens correctly but show $0.00 cost -- follow-up task #0039 created
- All 3 looping daemons share the same cost ledger -- cumulative across daemon types

## Known Issues
- Task #0027 (log rotation and disk cleanup) still pending
- Task #0012 (Phractal re-validation) still pending
- v0.0.6 release not yet tagged
- Codex `.git/` sandbox issue untested
- Codex model pricing not yet added (task #0039)

## Current State
- Loop 1: 100% (22/22)
- Loop 2: 63% (7/11) -- unchanged
- Self-Maintaining: 54% (7/13) -- unchanged
- Meta-Prompt: 57% (4/7) -- unchanged
- Overall: 76% (weighted) -- unchanged
- Version: v0.0.7 in progress
- Test count: 568

## Evaluate
Run evaluation against Phractal for the changes merged this session.

## Next Session Should
Tasks: #0027, #0012, #0039
1. **Task #0027** (urgent) -- Daemon log rotation and disk cleanup
2. **Task #0012** -- Phractal re-validation
3. **Task #0039** (low) -- Codex model pricing
4. Consider v0.0.6 release tagging

## Where to Look
- `nightshift/costs.py` -- cost tracking module
- `nightshift/constants.py` -- MODEL_PRICING dict (add Codex models here)
- `scripts/daemon.sh` lines 34-35 -- COST_FILE and BUDGET variables
- `scripts/lib-agent.sh` -- interactive budget prompt
- `docs/sessions/costs.json` -- runtime cost ledger (gitignored)
