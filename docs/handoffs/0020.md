# Handoff #0020
**Date**: 2026-04-04
**Version**: v0.0.7 in progress
**Session duration**: ~20m

## What I Built
- **Task #0035** (configurable model + effort for command_for_agent): All three command builders (`command_for_agent`, `plan_command_for_agent`, `_build_subagent_command`) now read model/effort/thinking from `NightshiftConfig` instead of hardcoding. Codex flags updated from old approval_policy pattern to `--dangerously-bypass-approvals-and-sandbox`. Environment variable overrides supported (config file > env var > default).
- Files modified: `nightshift/types.py`, `nightshift/constants.py`, `nightshift/config.py`, `nightshift/cycle.py`, `nightshift/planner.py`, `nightshift/subagent.py`, `nightshift/cli.py`, `nightshift/feature.py`, `nightshift/integrator.py`, `nightshift/profiler.py`, `.nightshift.json.example`, `tests/test_nightshift.py`
- New config keys: `claude_model`, `claude_effort`, `codex_model`, `codex_thinking`
- New env vars: `NIGHTSHIFT_CLAUDE_MODEL`, `NIGHTSHIFT_CODEX_MODEL`, `NIGHTSHIFT_CODEX_THINKING`
- Tests: 536 total passing (8 new tests for model config)

## Decisions Made
- Config priority chain: config file > env var > DEFAULT_CONFIG (matches lib-agent.sh pattern)
- `config: NightshiftConfig` threaded through all command builders and their callers rather than reading config inside the builders (keeps functions pure/testable)
- Codex `--json` flag kept alongside `--dangerously-bypass-approvals-and-sandbox` (output parsing depends on it)
- `claude_effort` not overridable via env var (only model names are -- effort is unlikely to change per-environment)

## Known Issues
- Task #0034 (interactive daemon setup) still pending
- Task #0026 (cost tracking and budget ceiling) still pending
- Task #0012 (Phractal re-validation) still pending
- v0.0.6 release not yet tagged
- Codex `.git/` sandbox issue untested

## Current State
- Loop 1: 100% (22/22)
- Loop 2: 63% (7/11) -- unchanged
- Self-Maintaining: 54% (7/13) -- unchanged
- Meta-Prompt: 57% (4/7) -- unchanged
- Overall: 76% (weighted) -- unchanged
- Version: v0.0.7 in progress
- Test count: 536

## Next Session Should
Tasks: #0034, #0026, #0027, #0012
1. **Task #0034** (urgent) -- Interactive daemon setup (prompt for agent/duration)
2. **Task #0026** (urgent) -- Cost tracking and budget ceiling
3. **Task #0027** -- Shift-level summary report
4. Consider v0.0.6 release tagging

## Where to Look
- `nightshift/cycle.py:command_for_agent()` -- Claude/Codex command construction with config
- `nightshift/config.py:merge_config()` -- env var override block (`_ENV_OVERRIDES`)
- `nightshift/constants.py:DEFAULT_CONFIG` -- default model values
- `scripts/lib-agent.sh` -- reference implementation (daemons use this, product uses nightshift/)
