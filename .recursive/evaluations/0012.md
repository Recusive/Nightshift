# Evaluation #0012

**Date**: 2026-04-05
**Target**: Phractal
**Agent**: claude
**Cycles**: 2
**After task**: #0098, #0121

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Startup | 8/10 | The prescribed default command started cleanly from the Nightshift repo root, created the worktree in the cloned repo, and finished without any rerun overrides. Startup is still capped because the run had no inferred verification command, so baseline verification never actually ran. |
| Discovery | 7/10 | The run found two concrete issues in different areas of the target repo: a backend CORS configuration bug and a frontend resize-handling bug. |
| Fix quality | 8/10 | Both fixes were small, coherent, and low-blast-radius, and the state/runner artifacts preserved them cleanly. This still stops short of full confidence because the target repo had no real verification command configured. |
| Shift log | 6/10 | The human-readable log now exists at the real `Docs/Nightshift/2026-04-05.md` path and contains a usable summary, fix details, and recommendations. It still has fidelity issues: cycle 2's logged commit hash (`37ce9c9`) drifted from the state/runner record (`88e4995`). |
| State file | 9/10 | `Docs/Nightshift/2026-04-05.state.json` is valid JSON, records both cycles, and preserves the fixes plus per-cycle verification metadata. The only real gap is that verification stayed `skipped` because `verify_command` remained null. |
| Verification | 3/10 | Baseline and cycle verification were skipped because Phractal still has no target-specific evaluation verify command. This remains the main production-readiness gap for real-repo evals. |
| Guard rails | 10/10 | The runner respected the cycle/file limits, stayed inside the cloned repo, and completed two cycles without touching blocked paths. |
| Clean state | 3/10 | The cloned repo still ended dirty: `git -C /private/tmp/nightshift-eval.qQx4vz status --short` reported `?? Docs/Nightshift/` after the run. |
| Breadth | 6/10 | The run covered two categories across two top-level areas (`apps` and `Orbit`), which is better than a single-path sweep but still narrow for a two-cycle evaluation. |
| Usefulness | 6/10 | A day-team reviewer can now read the real shift log directly, but skipped verification plus the dirty clone mean the evaluation still needs manual skepticism instead of being a trustworthy green signal. |
| **Total** | **66/100** | |

## Tasks Created

- None new. Existing task coverage already matches the reproduced failures:
  - `#0099` tracks the missing Phractal verification command.
  - `#0100` tracks dirty clones after rejected/cleanup paths.
  - `#0101` and `#0102` already cover the remaining human-readable/scoring fidelity gaps surfaced by the dated shift-log path and commit-hash drift evidence.

## Raw Evidence

- Default run: `PYTHONPATH=$(pwd) python3 -m nightshift test --agent claude --cycles 2 --cycle-minutes 5 --repo-dir /tmp/nightshift-eval.qQx4vz`
- Shift log: `/private/tmp/nightshift-eval.qQx4vz/Docs/Nightshift/2026-04-05.md`
- State file: `/private/tmp/nightshift-eval.qQx4vz/Docs/Nightshift/2026-04-05.state.json`
- Runner log: `/private/tmp/nightshift-eval.qQx4vz/Docs/Nightshift/2026-04-05.runner.log`
- Clean-state check: `git -C /private/tmp/nightshift-eval.qQx4vz status --short` returned `?? Docs/Nightshift/`
- Parser gap note: `nightshift.evaluation.parse_shift_artifacts()` still only searches `SHIFT-LOG*.md`, so the automated scorer under-read this run's dated `Docs/Nightshift/2026-04-05.md` artifact even though the human-readable log exists.
