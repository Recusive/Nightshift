# Evaluation #0019

**Date**: 2026-04-09
**Target**: Phractal (github.com/fazxes/Phractal)
**Agent**: claude
**Cycles**: 2
**After tasks**: #0260 (count-only payload regression fix in state.py, cycle.py, eval_runner.py) -- 1 session stale since eval #0091 (84/100). Primary goal: confirm the count-only regression is resolved.
**Eval gate status**: CLEAR (score >= 80 threshold met)

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Startup | 10/10 | Auto-clone feature worked correctly: `--repo-dir /tmp/nightshift-eval-0019` did not exist, nightshift cloned Phractal automatically with no manual pre-step. Worktree created at expected runtime path. Baseline verification (`python3 -m compileall apps/api/app`) passed with exit code 0 before any cycle started. |
| Discovery | 8/10 | Both cycles found real, distinct issues. Cycle 1: JWT cache collision risk in `fast_auth.py` -- `_get_token_hash` truncated tokens to first/last 20 chars before hashing, discarding payload entropy and creating a collision risk for tokens from the same issuer. Cycle 2: logging bug in `prompt_validator.py` -- `sanitize` method logged post-truncation length instead of original length, producing misleading monitoring output. Both issues are well-scoped with file references in the shift log. |
| Fix quality | 8/10 | Both fixes are narrow and low-blast-radius (1 file each). Security fix (fast_auth.py) changes `_get_token_hash` to hash the full token string instead of a truncated version -- a correct mitigation. Code Quality fix (prompt_validator.py) captures `original_length = len(prompt)` before truncation to fix misleading log output. Each fix entry in the state file includes title, category, impact, files, and commit hash. |
| Shift log | 9/10 | Durable human-readable artifact at `Docs/Nightshift/2026-04-09.md` survived the full run inside the worktree. Contains Summary, Stats, numbered Fixes with Category/Impact/Files/Verification/Commit, an empty Logged Issues section, and 3 actionable Recommendations (cache size bounding, API client error handling, prompt validator test coverage). Shift log co-committed alongside fix files in both cycles. |
| State file | 10/10 | State JSON is valid and fully structured. Both cycles have structured `fixes[]` objects with all required fields (title, category, impact, files, commit). `category_counts={"Code Quality": 1, "Security": 1}` is populated correctly. Both cycles have `status="completed"`. Counters: fixes=2, files_touched=2, agent_failures=0, failed_verifications=0 -- all match actual output. `halt_reason=null`. This is the direct confirmation that PR #262 (task #0260) fully resolved the count-only payload regression. |
| Verification | 10/10 | Baseline verification passed before cycles started. Both Cycle 1 and Cycle 2 post-cycle verification ran `python3 -m compileall apps/api/app` with exit code 0. No verification failures. Diff scores: Cycle 1 = 8/10 (Security), Cycle 2 = 8/10 (Code Quality). |
| Guard rails | 9/10 | No false rejections. Both cycles were accepted. violations=[] for both cycles. Structured fix objects captured correctly (the prior count-only payload bypass is now fixed). Prior-cycle-paths guard partially active: both cycles ended up in `apps/` area, though in different subdirectories (`middleware/` vs `security/`). |
| Clean state | 9/10 | `git -C /tmp/nightshift-eval-0019 status --short` returned no output (clean). Run completed normally with halt_reason=null. No non-zero exit. Worktree was isolated correctly in temp runtime dir. |
| Breadth | 7/10 | Both cycles worked within the `apps/api/app/` subtree: Cycle 1 in `middleware/`, Cycle 2 in `security/`. The prior-cycle-paths guard shows `["apps", "apps"]` for both cycles, meaning Cycle 2 was directed away from `apps` but still stayed in `apps/`. The guard prevented exact path repetition but did not push the second cycle to a new top-level area (e.g., Orbit, recursive-labs-website). Same overall score as eval #0017 on this dimension. |
| Usefulness | 9/10 | A developer reading `Docs/Nightshift/2026-04-09.md` can immediately understand what was fixed, which files changed, how to verify, and what patterns to audit further. The recommendations section identifies three concrete follow-up items (cache size bound, API client error unification, prompt validator tests). State file is now fully accurate and trustworthy for tooling. Improvement over prior evals where state file fix counter mismatch reduced tooling usefulness. |
| **Total** | **89/100** | Eval gate CLEAR. Score exceeds previous best (86/100 in eval #0016). |

## Delta vs Evaluation #0091 (84/100)

| Dimension | #0091 | #0019 | Change |
|-----------|-------|-------|--------|
| Startup | 10 | 10 | 0 |
| Discovery | 8 | 8 | 0 |
| Fix quality | 8 | 8 | 0 |
| Shift log | 9 | 9 | 0 |
| State file | 6 | 10 | **+4** |
| Verification | 10 | 10 | 0 |
| Guard rails | 8 | 9 | +1 |
| Clean state | 9 | 9 | 0 |
| Breadth | 8 | 7 | -1 |
| Usefulness | 8 | 9 | +1 |
| **Total** | **84** | **89** | **+5** |

## Count-Only Payload Fix Validation

PR #262 (task #0260) fixed the count-only payload regression in state.py, cycle.py, and eval_runner.py. This eval confirms the fix works end-to-end:

- `cycles[0].fixes = [{"title": "Hash full JWT token...", "category": "Security", ...}]` -- structured, not empty
- `cycles[1].fixes = [{"title": "Fix incorrect length...", "category": "Code Quality", ...}]` -- structured, not empty
- `category_counts = {"Code Quality": 1, "Security": 1}` -- populated correctly
- Both cycles `status = "completed"` -- not "unknown"
- State file counters match actual fix count (fixes=2 matches 2 structured fix objects)

Compare to eval #0091 where `cycles[*].fixes=[]`, `category_counts={}`, and `status="unknown"` for both cycles. The regression is fully resolved. The eval_runner.py 5-tier scoring ladder awards this result 10/10 (structured fixes + category_counts populated).

## Eval Gate Status

Score 89/100 exceeds the BUILD EVAL GATE threshold of >= 80. This is the highest score since eval #0016 (86/100) and beats the target of >= 85 set in the handoff. Normal BUILD tasks may resume.

## Raw Evidence

- Default run command: `python3 -m nightshift test --agent claude --cycles 2 --cycle-minutes 5 --repo-dir /tmp/nightshift-eval-0019`
- No manual pre-clone required (auto-clone worked)
- Runtime dir: `/var/folders/dm/48s5j0qs7yq4nvq9s61jcbqw0000gn/T/nightshift-test-runs/nightshift-eval-0019-b7c03211db7c/`
- State file: `{runtime_dir}/2026-04-09.state.json`
- Runner log: `{runtime_dir}/2026-04-09.runner.log`
- Shift log: `{runtime_dir}/worktree-2026-04-09/Docs/Nightshift/2026-04-09.md`
- Cycle 1 fix commit: `a879618634bd435e82231f2cc4cef69ea7ee1e41` (Security, apps/api/app/middleware/fast_auth.py)
- Cycle 2 fix commit: `67f9b77a359680f85c3df363149f00ee981a32eb` (Code Quality, apps/api/app/security/prompt_validator.py)
- Clone cleanliness: `git -C /tmp/nightshift-eval-0019 status --short` returned no output
- Diff scores: Cycle 1 = 8/10 (Security), Cycle 2 = 8/10 (Code Quality)
- State counters: fixes=2, files_touched=2, agent_failures=0, failed_verifications=0
- Structured fix objects: Cycle 1 fixes=[{Security, fast_auth.py}], Cycle 2 fixes=[{Code Quality, prompt_validator.py}]
- category_counts={"Code Quality": 1, "Security": 1} (populated, regression resolved)
- recent_cycle_paths=["apps", "apps"] (breadth guard partially effective)
- halt_reason=null

## Tasks Created

- None. Score 89/100 exceeds the eval gate. No dimension scored below 6/10.
- Breadth scored 7/10 (both cycles in apps/ subtree). Existing breadth improvement work may be relevant but no new task needed at this threshold.
- The prior task #0247 (count-only payload regression) is now resolved -- this eval confirms the fix. No follow-up needed.
