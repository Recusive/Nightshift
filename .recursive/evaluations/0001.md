# Evaluation #0001
**Date**: 2026-04-05
**Target**: Phractal
**Agent**: claude
**Cycles**: 2
**After task**: #0018 (Enhance profiler with deeper analysis)

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Startup | 4/10 | Default evaluation startup failed twice before the scored run: inherited `CLAUDECODE=1` blocked nested Claude sessions, then the default `claude_effort=max` setting was rejected. After manual env/config overrides the runner created a worktree and executed both cycles. |
| Discovery | 7/10 | Cycle 1 found a real JWT cache-key collision in `apps/api/app/middleware/fast_auth.py`. Cycle 2 found a real auth error-disclosure issue in `apps/api/app/api/v1/auth.py` and logged three additional issues in `apps/web/lib/api-client.ts` and `apps/web/lib/supabase/server.ts`. |
| Fix quality | 6/10 | Both fixes were small, plausible security changes with clear commit messages. Neither change had tests, and the JWT cache-key change should gain regression coverage before being trusted in production. |
| Shift log | 3/10 | Claude wrote useful shift-log entries, but committed them to `Docs/Nightshift/2026-04-05.md`. The verifier expects `docs/Nightshift/2026-04-05.md`, so both cycles were rejected and the official shift log stayed a template. |
| State file | 8/10 | `2026-04-05.state.json` is valid JSON and captures both rejected cycles, commit hashes, touched files, and the halt reason. |
| Verification | 2/10 | Evaluation ran with `verify_command: null`, so baseline verification was skipped and no repo verification ran after either cycle. Only shift-log policy checks executed. |
| Guard rails | 8/10 | File-count limits were respected, blocked paths were not touched, and the runner rejected invalid cycles instead of accepting them. |
| Clean state | 3/10 | The worktree itself reset, but the target clone was left dirty with untracked `.nightshift.json` and `Docs/Nightshift/` artifacts after the rejected run, and the session halted on failed verification threshold. |
| Breadth | 6/10 | The run covered API auth middleware, auth routes, and logged issues in web auth/api client code. That is reasonable breadth for a 2-cycle validation run, but still concentrated around auth/session surfaces. |
| Usefulness | 4/10 | There were actionable findings in the runner/state artifacts, but the day-team-facing shift log remained template-only and no fixes survived acceptance, so the final output was hard to consume. |
| **Total** | **51/100** | |

## Tasks Created
- #0097: Evaluation #0001: harden Claude evaluation startup
- #0098: Evaluation #0001: normalize shift-log paths on case-insensitive filesystems
- #0099: Evaluation #0001: add a real verification command for Phractal evals
- #0100: Evaluation #0001: leave evaluation clones clean after rejected cycles
- #0101: Evaluation #0001: preserve rejected-cycle findings in human-readable output

## Raw Evidence
- Final scored run used `/tmp/nightshift-eval-20260405c` with `env -u CLAUDECODE` and a temporary `.nightshift.json` override of `{ "verify_command": null, "claude_effort": "high" }`.
- Shift log: `/tmp/nightshift-eval-20260405c/docs/Nightshift/2026-04-05.md` remained the template file, while rejected commits wrote real content to `/tmp/nightshift-eval-20260405c/Docs/Nightshift/2026-04-05.md`.
- State file: `/tmp/nightshift-eval-20260405c/docs/Nightshift/2026-04-05.state.json` recorded both cycles as rejected with violation `No commit in this cycle includes the shift log update.` even though commits `1ea27a9` and `7ac6bd9` updated `Docs/Nightshift/2026-04-05.md`.
- Runner log: `/tmp/nightshift-eval-20260405c/docs/Nightshift/2026-04-05.runner.log` showed two earlier startup failures (`CLAUDECODE=1` nested-session block, then unsupported `--effort max`) before the scored run executed both cycles.
