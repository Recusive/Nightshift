# Evaluation #0017

**Date**: 2026-04-09
**Target**: Phractal (github.com/fazxes/Phractal)
**Agent**: claude
**Cycles**: 2
**After tasks**: 14 sessions since eval #0016 -- changes to owl/cycle.py (security hardening), raven/feature.py (IndexError fix), raven/coordination.py (conflict detection), owl/scoring.py, owl/readiness.py (path traversal guard), owl/eval_runner.py (new module), infra/release.py (new module), infra/module_map.py (session labeling)
**Eval gate status**: CLEAR (score >= 80 threshold met)

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Startup | 9/10 | Default command required a manual pre-clone step (`git clone` before `nightshift test`) because the tool validates the repo dir exists before cloning. Worktree created at expected runtime path. Baseline verification (`python3 -m compileall apps/api/app`) passed with exit code 0 before any cycle started. No other manual overrides needed. |
| Discovery | 8/10 | Both cycles found real, distinct issues: Cycle 1 identified an information-leakage anti-pattern (`detail=str(e)`) in the `/register` auth endpoint (same class as the previous `auth.py` pattern, different endpoint); Cycle 2 found an accessibility gap in the Orbit theme-toggle button (ASCII art only, no aria-label). Both issues are well-scoped with file and line references present in the shift log. |
| Fix quality | 8/10 | Both fixes are narrow and low-blast-radius (1 file each). Security fix (auth.py) replaces `str(e)` with a generic "Registration failed" message, consistent with the existing `/login` handler. A11y fix (page.tsx) adds a dynamic `aria-label` that reflects the current action ("Switch to light/dark theme"). Each fix entry includes Category, Files, Impact, Commit, and Verification fields. |
| Shift log | 9/10 | Durable human-readable artifact at `Docs/Nightshift/2026-04-09.md` survived the full run inside the worktree. Contains Summary, Stats, numbered Fixes with Impact/Verification, an empty Logged Issues section, and Recommendations. Shift log co-committed alongside fix files in both cycles (separate shift-log-update commits follow each fix commit). |
| State file | 7/10 | State JSON at runtime dir is valid and structurally complete. counters.fixes=3 does not match the 2 fixes in the shift log or the 1 structured fix object in cycles[*].fixes (Cycle 1 fixes[] is empty; only Cycle 2 has structured fix data). Cycle 1 used a count-based JSON response schema that did not populate fixes[] -- same shape as the pre-PR-#167 count-only payload bug. Other counters are plausible: files_touched=2, agent_failures=0, failed_verifications=0. The mismatch between counter and structured data is a scoring concern. |
| Verification | 10/10 | Baseline verification passed before cycles started. Both Cycle 1 and Cycle 2 post-cycle verification ran `python3 -m compileall apps/api/app` with exit code 0. No verification failures. Diff scores: Cycle 1 = 8/10 (Security), Cycle 2 = 8/10 (A11y). |
| Guard rails | 8/10 | No false rejections. Both cycles were accepted. violations=[] for both cycles. However, Cycle 1 returned a count-based JSON payload instead of a structured fix list, which means the fix object was not stored in cycles[1].fixes[] -- this is the same failure mode PR #167 was meant to fix. The fix still committed and verified, but the structured data was not captured. |
| Clean state | 9/10 | `git -C /tmp/nightshift-eval-0017 status --short` returned no output (clean). Run completed normally with halt_reason=null. No non-zero exit. |
| Breadth | 7/10 | Cycle 1 worked in `apps/api/app/api/v1/` (backend auth), Cycle 2 worked in `Orbit/app/` (frontend UI). Two distinct top-level areas covered. Both stayed in lower blast-radius files. The short 2-cycle window limits breadth; the prior-cycle-paths guard worked correctly (Cycle 2 avoided `apps`). |
| Usefulness | 8/10 | A developer reading `Docs/Nightshift/2026-04-09.md` can immediately understand what was fixed, which files changed, how to verify, and what patterns to audit further. Recommendations section identifies the `detail=str(e)` pattern breadth and two separate A11y gaps. State file's fix counter mismatch (3 vs 2) would cause confusion for tooling that relies on it. |
| **Total** | **83/100** | Eval gate CLEAR. |

## Delta vs Evaluation #0016 (86/100)

| Dimension | #0016 | #0017 | Change |
|-----------|-------|-------|--------|
| Startup | 9 | 9 | 0 |
| Discovery | 8 | 8 | 0 |
| Fix quality | 8 | 8 | 0 |
| Shift log | 9 | 9 | 0 |
| State file | 9 | 7 | -2 |
| Verification | 10 | 10 | 0 |
| Guard rails | 9 | 8 | -1 |
| Clean state | 9 | 9 | 0 |
| Breadth | 7 | 7 | 0 |
| Usefulness | 8 | 8 | 0 |
| **Total** | **86** | **83** | **-3** |

## Root Cause of Regression

The -3 point regression traces to a single recurring issue: Cycle 1's count-based JSON response was not parsed into structured fix objects in the state file. This is the same `count-only payload` failure mode that PR #167 (task #0139) addressed. However, the fix appears to have regressed or is not fully covering this case:

- `cycles[0].fixes = []` (empty) despite the fix committing successfully
- `counters.fixes = 3` (inflated vs. the 2 actual fixes and 2 shift-log commits)

The fix still committed and verified (Diff score: 8/10), so the regression is in state accuracy, not cycle execution. The state file counter inflation (3 vs 2) is consistent with the counter tracking commits rather than structured fix objects.

## Eval Gate Status

Score 83/100 exceeds the BUILD EVAL GATE threshold of >= 80. Normal BUILD tasks may resume.

## Raw Evidence

- Default run command: `PYTHONPATH=$(pwd) python3 -m nightshift test --agent claude --cycles 2 --cycle-minutes 5 --repo-dir /tmp/nightshift-eval-0017`
- Pre-step required: `git clone https://github.com/fazxes/Phractal /tmp/nightshift-eval-0017`
- Runtime dir: `/var/folders/dm/48s5j0qs7yq4nvq9s61jcbqw0000gn/T/nightshift-test-runs/nightshift-eval-0017-487c0d852419/`
- State file: `{runtime_dir}/2026-04-09.state.json`
- Runner log: `{runtime_dir}/2026-04-09.runner.log`
- Shift log: `{runtime_dir}/worktree-2026-04-09/Docs/Nightshift/2026-04-09.md`
- Cycle 1 fix commit: `1dab477` (Security fix, apps/api/app/api/v1/auth.py)
- Cycle 2 fix commit: `0ec5014` (A11y fix, Orbit/app/page.tsx)
- Clone cleanliness: `git -C /tmp/nightshift-eval-0017 status --short` returned no output
- Diff scores: Cycle 1 = 8/10 (Security), Cycle 2 = 8/10 (A11y)
- State counters: fixes=3, files_touched=2, agent_failures=0, failed_verifications=0
- Structured fix objects: Cycle 1 fixes=[], Cycle 2 fixes=[{A11y, Orbit/app/page.tsx}]
- No manual overrides used beyond pre-clone

## Tasks Created

- Task #0247: Investigate count-only payload regression in Cycle 1 state file (State file 7/10, Guard rails 8/10) -- the `cycles[0].fixes=[]` empty list despite a committed fix indicates `parse_cycle_result()` is still not capturing Cycle 1's structured output correctly; may be a schema mismatch in the count-vs-list detection path.
- Task #0248: Auto-clone target repo if missing in `nightshift test` command (Startup 9/10) -- the tool exits with FileNotFoundError when `--repo-dir` does not exist; it should clone from the configured eval target URL automatically, matching what the task description implies.
