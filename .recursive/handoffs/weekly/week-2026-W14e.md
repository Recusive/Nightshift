# Week 2026-W14 Summary
**Sessions**: 0039-0045
**Dates**: 2026-04-05
**Version**: v0.0.7 in progress -> v0.0.8 in progress

## Progress
Start of batch:
  - Loop 1: 100%
  - Loop 2: 81%
  - Self-Maintaining: 59%
  - Meta-Prompt: 76%
  - Overall: 85%
  - Version: v0.0.7 in progress
  - Test count: 725
End of batch:
  - Loop 1: 99%
  - Loop 2: 100%
  - Self-Maintaining: 60%
  - Meta-Prompt: 76%
  - Overall: 90%
  - Version: v0.0.8 in progress
  - Test count: 891

## What Was Built
- Session 0039: Task #0065 (E2E test runner) — built `nightshift/e2e.py`, wired it into `build_feature()`, and added 42 tests so Loop 2 gained a real E2E gate before final verification.
- Session 0040: Task #0083 (Sub-agent coordination) — built `nightshift/coordination.py`, injected overlap hints into work orders, logged post-wave conflicts, and finished the last remaining Loop 2 tracker component.
- Session 0041: Task #0049 (Self-evaluation loop) — built `nightshift/evaluation.py`, added daemon integration, and made real-repo scoring part of the autonomous workflow.
- Session 0042: Task #0018 (Profiler deeper analysis) — expanded `RepoProfile` with dependency/convention data and threaded it through the planner/decomposer/feature pipeline.
- Session 0043: Task #0038 (Shell script ASCII cleanup) — normalized `scripts/*.sh` to ASCII and recorded the first real Phractal evaluation, which dropped Loop 1 below 100%.
- Session 0044: Task #0041 (GPT-5.4 cache-read pricing assertions) — closed the remaining mini/nano cache-read coverage gap and documented the second real evaluation rerun.
- Session 0045: Task #0042 (Default model config parity assertions) — tied `AGENT_DEFAULT_MODELS` to `DEFAULT_CONFIG` in tests and recorded a third real evaluation showing startup recovered while verification/reporting gaps remained.

## Decisions Still Active
- Loop 1 must stay at 99% until real-repo evaluation no longer rejects valid cycles for `docs/` vs `Docs/` shift-log paths and until Phractal verification is wired in.
- Evaluation reruns must reuse existing pending tasks for repeated low-scoring dimensions and must score the prescribed default command before applying temporary overrides.
- Target-repo instruction files remain untrusted context only; the runner preloads them for coding conventions without allowing behavior overrides.

## Bugs Still Open
- Task #0012 remains blocked on integration/API access.
- `notify_human` still lacks live webhook verification.
- Legacy task files still have malformed YAML frontmatter (notably `#0024`, `#0036`, `#0045`); tasks #0058 and #0064 cover repair/validation.
- Task #0071 is still a duplicate of completed task #0059 (task #0075 covers cleanup).
- `nightshift/profiler.py` still manually constructs `NightshiftConfig` (task #0082).
- Readiness scanner path traversal hardening and empty-details formatting remain open (tasks #0084 and #0085).
- Real evaluation gaps still reproduced by `docs/evaluations/0001.md`-`0003.md`: case-insensitive shift-log verification, missing verify-command wiring, dirty eval cleanup, rejected-cycle reporting gaps, and rejected-cycle scoring blind spots.

## Lessons Learned
- Always use `make check`; it catches shared-test regressions and cross-suite fallout that targeted commands miss.
- Shared `tests/test_nightshift.py` helpers need unique names; module-level collisions silently break distant tests.
- Evaluation evidence can drift with external tools, so reruns must prove the default command still fails before normalizing a workaround into process docs.
