---
status: pending
priority: normal
target: v0.0.8
vision_section: meta-prompt
created: 2026-04-05
source: github-issue-61
completed:
---

# Create AGENTS.md mirroring CLAUDE.md

Create an AGENTS.md file at the repo root that mirrors CLAUDE.md. Some AI tools (Codex, Copilot) read AGENTS.md instead of CLAUDE.md. Both files should contain the same instructions so any agent landing in this repo gets the same context regardless of which tool they use.

Keep them in sync — if one changes, the other should too. Add a note at the top of both files pointing to the other.

## Note (2026-04-06)

`AGENTS.md` was added to `PROMPT_GUARD_FILES` in `scripts/lib-agent.sh` as
part of the prompt-guard blind-spot fix (handoff #0072). The guard handles
missing files gracefully, so this causes no false positives before the file
exists. When implementing this task, no additional PROMPT_GUARD_FILES change
is needed -- just create the file and update CLAUDE.md as described below.

## Acceptance Criteria
- AGENTS.md exists at repo root with same content as CLAUDE.md
- Both files have a note at top: "This file is mirrored in AGENTS.md / CLAUDE.md -- keep both in sync"
- CLAUDE.md editing conventions section mentions AGENTS.md sync requirement
- (PROMPT_GUARD_FILES already includes AGENTS.md -- no shell change needed)
