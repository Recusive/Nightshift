---
status: done
priority: normal
target: v0.0.6
created: 2026-04-03
completed: 2026-04-03
---

# Build sub-agent spawner module

With the decomposer complete, build the sub-agent spawner -- the component that takes a list of WorkOrders for a single wave and executes them against the target repo using codex or claude CLI.

See docs/vision/02-loop2-feature-builder.md "Sub-Agent Execution" for spec.

## Acceptance Criteria
- [ ] `nightshift/subagent.py` exists with `spawn_wave()` function
- [ ] Takes a list of WorkOrders + agent type + repo path, returns list of task completion results
- [ ] Each sub-agent gets its own worktree (or runs sequentially in the same worktree)
- [ ] Parses agent output using task.schema.json structure
- [ ] Handles agent failures gracefully (marks task as blocked)
- [ ] Respects DECOMPOSER_MAX_RETRIES constant
- [ ] Tests cover spawner logic (mocked agent invocations)
- [ ] Vision tracker updated
