---
status: done
priority: normal
target: v0.0.8
vision_section: self-maintaining
created: 2026-04-09
source: strategy-report-0123
completed: 2026-04-09
---

# Run nightshift against Phractal immediately: produce eval #0017

## Problem

The last E2E validation of nightshift against Phractal was eval #0016 in session #0108 -- 14 sessions ago. Since then, the following nightshift modules changed: owl/cycle.py (security hardening), raven/feature.py (IndexError fix), raven/coordination.py (conflict detection), owl/scoring.py, owl/readiness.py (path traversal guard), owl/eval_runner.py (new module), infra/release.py (new module), infra/module_map.py (session labeling).

The tool is built but never used. The system cannot know whether these 14 sessions of changes improved, degraded, or broke Phractal compatibility.

## What To Do

The brain should delegate this as a build task:

1. Run `PYTHONPATH=$(pwd) python3 -m nightshift test --agent claude --cycles 2 --cycle-minutes 5 --repo-dir /tmp/nightshift-eval-0017` against `https://github.com/fazxes/Phractal`.
2. Score each dimension using the eval rubric (Startup, Discovery, Fix quality, Shift log, State file, Verification, Guard rails, Clean state, Breadth, Usefulness).
3. Write the result to `.recursive/evaluations/0017.md` following the format of eval #0016.
4. If any dimension scores below 7/10, create a follow-up task for each gap.
5. If total score is below 80/100, set eval gate status to BLOCKED and create an urgent task.

## Acceptance Criteria

- `.recursive/evaluations/0017.md` exists with a complete score table
- Score >= 80/100 (gate CLEAR), or urgent task created if below
- Shift log artifact exists in the Phractal repo's Docs/ directory
- `/tmp/nightshift-eval-0017` clone is cleaned up after the run
- Vision tracker updated: `Post-cycle verification` progress reflects the result
