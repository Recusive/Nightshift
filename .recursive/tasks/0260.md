---
status: pending
priority: low
target: v0.0.8
vision_section: loop1
source: code-review
created: 2026-04-09
completed:
---

# Investigate count-only payload regression (new task since #0247 is archived done)

## Problem

Eval #0091 (84/100) confirms the count-only payload regression persists:
- `cycles[*].fixes=[]` empty for both cycles despite fixes committing
- `cycles[*].status="unknown"` for both cycles
- `category_counts={}` empty

Task #0247 was previously created for this but was archived as done. The fix for #0247 evidently did not eliminate the regression (State file dimension scored 6/10 in eval #0091, same as the pattern observed in eval #0017).

This is the primary remaining obstacle to Loop 1 reaching 100%.

## Context

The regression is in state metadata accuracy only -- cycle execution, verification, shift log generation, and diff scoring all work correctly. The issue is that structured fix objects are not being captured in the state JSON.

## Acceptance Criteria

- [ ] Identify why `cycles[*].fixes` is empty when fixes are committed
- [ ] Fix the root cause so structured fix metadata is captured
- [ ] `category_counts` populated correctly after fix
- [ ] Eval rerun shows State file dimension improvement (target: 8+/10)
- [ ] `make check` passes
