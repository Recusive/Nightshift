---
status: pending
priority: low
target: v0.0.9
vision_section: self-maintaining
created: 2026-04-06
source: pentest-watch-0089
completed:
---

# Add scripts/run.sh and scripts/test.sh to PROMPT_GUARD_FILES

## Context

Pentest scan (session 0089) noted that `scripts/run.sh` and
`scripts/test.sh` are not in `PROMPT_GUARD_FILES` in `lib-agent.sh`.
Both are thin PYTHONPATH wrappers. Modifications to these scripts would
redirect the Python module resolution path for human-initiated runs.

`PROMPT_GUARD_DIRS` covers `scripts/` for NEW files only. Low daemon
impact (daemon invokes Claude/Codex directly via `run_agent`, not through
these scripts) but human-run entry points are unprotected for modifications
to existing files.

## Acceptance Criteria

- [ ] `nightshift/scripts/run.sh` and `nightshift/scripts/test.sh` are added to `PROMPT_GUARD_FILES`
      in `Recursive/engine/lib-agent.sh`
- [ ] `.recursive.json` is added to `PROMPT_GUARD_FILES` in `Recursive/engine/lib-agent.sh`
      (merged from #0196 — defense-in-depth against eval_frequency/verify_command tampering)
- [ ] `bash -n Recursive/engine/lib-agent.sh` passes
- [ ] Existing prompt-guard test coverage still passes
- [ ] `make check` passes

## Note

Task #0196 (add .recursive.json to PROMPT_GUARD_FILES) was merged into this task by OVERSEE on 2026-04-09. Both are single-line additions to the same array in `lib-agent.sh`.
