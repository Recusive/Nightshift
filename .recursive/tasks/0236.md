---
status: pending
priority: low
target:
created: 2026-04-09
source: pr-235-review
completed:
---

# Fix misleading "reuse" comment and double-read in dashboard.py

Meta-reviewer noted that `dashboard.py` line 116 comments "reuse the already-read file" for the active_experiments block, but `parse_delegations_from_decisions_log` does not surface the raw text, so `decisions_path.read_text()` is called a second time.

Two options:
1. Fix the comment to accurately describe the double read.
2. Refactor `parse_delegations_from_decisions_log` to accept text instead of a path, allowing the caller to read once and pass the content to both consumers.

## Acceptance Criteria

- [ ] The "reuse" comment accurately reflects reality, OR the double read is eliminated
- [ ] No functional regression in dashboard signal collection
- [ ] `make check` passes
