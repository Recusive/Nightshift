---
status: blocked
priority: low
target: v0.0.8
vision_section: self-maintaining
created: 2026-04-08
completed:
blocked_reason: dependency
source: code-review-advisory
---

# Expand eval cycle count for higher Breadth score

**Blocked:** Depends on tasks #0094 (E2E validation wired into daemon), #0224 (brain runs nightshift against Phractal), and #0228 (brain re-runs eval after builds). Tuning eval cycle count is premature until the eval loop itself runs regularly. Unblock after those tasks are complete.

## Context

Eval #0016 scored Breadth at 7/10 with a note: "a longer run would explore more
dimensions." The 2-cycle eval window limits coverage to 2 top-level areas.

Running 3-4 cycles in the eval would push Breadth higher. Cycle 2's diff score
was 5/10 (A11y), suggesting the agent picks lower-impact fixes in later cycles.

## Acceptance Criteria

- [ ] Eval command updated to use --cycles 3 or 4 (or made configurable)
- [ ] A new eval report confirms Breadth >= 8/10 with the longer window
- [ ] No regression in other dimensions
