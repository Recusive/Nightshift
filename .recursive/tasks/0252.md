---
status: pending
priority: low
target: nightshift
source: code-review
created: 2026-04-09
completed:
---

# Fix stale _dependency_order docstring in module_map.py

## Problem

The `_dependency_order` docstring (around line 240 in `nightshift/infra/module_map.py`) says "imports from subpackages...that do not appear as standalone entries are ignored". This was true before PR #250, but is now incorrect -- subpackage entries ARE in `parsed_modules` and ARE tracked with slash keys (e.g. `core/constants`).

## What To Do

1. Update the `_dependency_order` docstring to accurately describe the new behavior with subpackage slash keys
2. Make check passes

## Acceptance Criteria

- Docstring accurately describes current behavior
- No code changes needed, just the docstring
