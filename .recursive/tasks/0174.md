---
status: pending
priority: normal
target: v0.0.9
vision_section: self-maintaining
created: 2026-04-06
completed:
---

# Add test: is_auth_failure returns 1 when pattern appears outside result/agent_message events

The `is_auth_failure()` function is scoped to `type:result` (Claude) and
`item.completed/agent_message` (Codex) events.  There is no test proving
that a "not logged in" string appearing in a different event type (e.g.,
a `text` block or a `tool_result`) does NOT trigger a false positive.

## Acceptance Criteria

- [ ] Add a regression test: a log where "not logged in" appears in a
      non-`result`, non-`agent_message` event body returns exit code 1
      from `is_auth_failure`
- [ ] The test class `TestAuthFailureDetection` in `test_nightshift.py`
      is the right place (PR #168 context)
