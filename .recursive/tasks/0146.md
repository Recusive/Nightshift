---
status: pending
priority: normal
target: v0.0.8
vision_section: self-maintaining
created: 2026-04-06
source: review-note-pr-135
completed:
---

# Add integration test: prompt-guard detects check.sh modification

## Context

**Path update (2026-04-07):** Scripts moved. `check.sh` is now at `nightshift/scripts/check.sh`.
PROMPT_GUARD_FILES is in `Recursive/engine/lib-agent.sh`.

## Acceptance Criteria

- A test (shell or Python) modifies `nightshift/scripts/check.sh` after
  `save_prompt_snapshots` runs and then calls `check_prompt_integrity`.
- The test verifies that `check_prompt_integrity` returns non-zero and writes
  an alert file containing a diff of the change.
- The test cleans up (restores original, removes temp files).
- `make check` continues to pass.
