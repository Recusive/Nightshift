---
status: pending
priority: low
target: v0.0.9
vision_section: self-maintaining
created: 2026-04-07
source: pentest
---

# Add .recursive.json to PROMPT_GUARD_FILES

## Context

Pentest scan (2026-04-07, THEORETICAL): `.recursive.json` is not in
`PROMPT_GUARD_FILES` in `Recursive/engine/lib-agent.sh`. An agent could modify
`eval_frequency`, project metadata, or `verify_command` and push to origin/main.

Mitigated by:
- PR workflow + code review (changes go through PRs)
- `reset_repo_state` restoring working copy each cycle
- Low exploitability (requires bypassing PR review)

Still worth guarding for defense-in-depth.

## Acceptance Criteria

- [ ] `.recursive.json` added to `PROMPT_GUARD_FILES` in `Recursive/engine/lib-agent.sh`
- [ ] `bash -n Recursive/engine/lib-agent.sh` passes
- [ ] Existing prompt-guard tests still pass
- [ ] `make check` passes
