---
status: pending
priority: low
target: recursive
vision_section: self-maintaining
created: 2026-04-08
source: code-review-advisory
completed:
---

# Add infra.release to CLAUDE.md dependency flow chain and normalize ordering

## Context

Code review advisory notes (PR #219):

1. CLAUDE.md dependency flow chain (line ~170) does not list `infra.release` in the ordered module chain. The module is registered in `__init__.py` and `install.sh` but the dependency flow documentation is incomplete.

2. CLAUDE.md infra/ listing uses non-alphabetical order (`worktree, module_map, multi, release`) while OPERATIONS.md uses alphabetical order (`module_map, multi, release, worktree`). Should normalize CLAUDE.md to alphabetical.

## Acceptance Criteria

- [ ] CLAUDE.md dependency flow chain includes `infra.release` at the correct position
- [ ] CLAUDE.md infra/ listing uses alphabetical order matching OPERATIONS.md
- [ ] No functional code changes
