---
status: pending
priority: low
target:
created: 2026-04-09
source: pr-237-review
completed:
---

# Add append_session_index_row call to budget-stop path in daemon.sh

Meta-reviewer noted that DAEMON.md documents a BUDGET-STOP row being written to sessions/index.md on budget halt, but no such call to `append_session_index_row` exists in the budget-stop path (daemon.sh just `break`s). Now that the function exists, wire it into the budget-stop exit.

## Acceptance Criteria

- [ ] `append_session_index_row` called before `break` in the budget-stop path
- [ ] Row includes appropriate status like "budget-stop"
- [ ] `make check` passes
