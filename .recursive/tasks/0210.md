---
status: done
priority: low
target: recursive
vision_section: none
created: 2026-04-08
source: code-review-advisory
completed: 2026-04-08
---

# Add pentest_framework_tasks to --with-signals safe_signals in pick-role.py

## Context

Meta-reviewer advisory note (PR #199): The `pentest_framework_tasks` signal was
added to the main `signals` dict and to the `--advise` JSON output, but NOT to
the `--with-signals` safe_signals block (pick-role.py lines 328-348). This means
the brain's `<system_signals>` context block will not include this signal.

The brain can see the value indirectly through the advisory's signals dict, but
for consistency and transparency, it should also appear in the safe_signals block.

## Acceptance Criteria

- [x] `"pentest_framework_tasks": signals["pentest_framework_tasks"]` added to safe_signals dict
- [x] Position: between `friction_entries` and `pending_tasks` for logical grouping
- [x] `make check` passes
