---
status: pending
priority: low
target: recursive
vision_section: self-maintaining
created: 2026-04-08
source: code-review-advisory
completed:
---

# Update CLAUDE.md and OPERATIONS.md for release.py module

## Context

Code review advisory note (PR #218): The new module checklist in CLAUDE.md requires updating
the structure tree when adding a new module. `nightshift/infra/release.py` was added but the
CLAUDE.md structure tree still reads `Infrastructure (worktree, module_map, multi)` without
mentioning `release`. OPERATIONS.md module table also needs updating.

## Acceptance Criteria

- [ ] CLAUDE.md structure tree updated: `infra/` line includes `release`
- [ ] OPERATIONS.md module table includes `release.py` entry
- [ ] No functional code changes
