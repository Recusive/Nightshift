---
status: pending
priority: low
target: v0.0.8
vision_section: self-maintaining
created: 2026-04-05
completed:
---

# Make scripts/list-tasks.sh directly executable or stop advertising direct invocation

Session `#0053` validated fragile automation paths and found that
`scripts/list-tasks.sh` works through `make tasks` but fails with
`permission denied` when invoked directly from the repo root because the file is
not executable.

## Acceptance Criteria
- [ ] `scripts/list-tasks.sh` can be run directly from the repo root without `permission denied`, or all docs/examples are updated to advertise only `bash scripts/list-tasks.sh` / `make tasks`
- [ ] `make tasks` still works after the fix
- [ ] Regression coverage or doc validation prevents the advertised invocation mode from drifting again
