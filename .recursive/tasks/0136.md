---
status: pending
priority: normal
target: v0.0.8
vision_section: loop1
created: 2026-04-06
source: overseer-decomposition-0029
completed:
---

# Honor pinned eval-target revisions in Step 0 and smoke test

Task `#0029` is externally blocked on selecting and refreshing a real Phractal
commit SHA, but Nightshift still needs internal plumbing so a chosen pin can be
consumed consistently once one exists.

## Acceptance Criteria
- [ ] Evaluation target metadata can express an optional pinned revision for a
      known repo without regressing the current unpinned behavior
- [ ] The Step 0 evaluation path and `scripts/smoke-test.sh` honor that pinned
      revision instead of cloning target HEAD when one is configured
- [ ] Regression coverage proves the default path is unchanged when no pinned
      revision is present

## Notes

If task `#0132` lands first, build on its structured metadata shape instead of
adding a second config format.
