# Codex project configuration for Recursive/Nightshift
# See: https://developers.openai.com/codex/config-reference

model = "gpt-5.4"
model_reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"
developer_instructions = "You are the Brain orchestrator. Read .codex/agents/brain.toml for your full identity, delegation protocol, agent catalog, and rules. Follow them exactly."

[agents]
max_threads = 6
max_depth = 1

[features]
multi_agent = true
